# 処理時間の測定 — 手順書

対象: whosaid-editor / 1号機（GPU あり）
音源: `01+02edited.wav`（67分16秒）
作成日: 2026-08-31

---

## 0. この測定で何を決めるのか

| 比較 | 何が分かるか |
| --- | --- |
| 測定1 対 測定2 | **出力先の差**（Google ドライブ 対 ローカルディスク） |
| 測定2 対 測定3 | **モデルの差**（標準 small 対 高精度 large-v3） |
| 各条件の 3 回 | **揺れの幅**。上の 2 つの差が、揺れを超えているかを判定する |

**揺れの幅が分からないと、差が実在するか判定できません。** 測定1が12分、測定2が17分でも、
同じ条件で3回回して12分〜18分に散らばるなら、その差は揺れの中に埋もれています。

揺れの原因は既知です（設計書 §9.5.7・温度の引き直しによる乱数）。**揺れること自体は
不具合ではありません。** 幅を知るために測ります。

---

## 1. Claude Code に渡す作業

### 1-1. ビルド（済）

モデル選択の UI 変更（`c2d4bf4`）を含むビルドを作る。

**2026-08-31 10:37:45 に完了。** `dist\WhosaidEditor\WhosaidEditor.exe`
（Inno Setup を入れていないので、インストーラは作らずポータブル版まで）。

### 1-2. キャッシュ削除のスクリプト（実装済み）

**`tools/clear_run_cache.py`。7回の測定で毎回実行します。**

> **この節は実装に合わせて書き直してあります（2026-08-31）。**
> 当初は「`transcripts/` を消す」としか書いていませんでしたが、
> **話者分離もキャッシュされる**ことが実装を読んで分かりました（下記）。

#### 消すもの・退避するもの・残すもの

| | 対象 | なぜ |
| --- | --- | --- |
| **消す** | `transcripts/` | 転写のキャッシュ。**これが本体。**残ると転写が 0 秒になる |
| **消す** | `diarize/` | **話者分離もキャッシュされる**（`diarize.py` の `load_turns`）。残ると話者分離が数秒で終わる |
| **消す** | `inspect/` | 聴く順番。作り直せる派生データ |
| **退避** | `<音声名>.speakers.json` | **消さずに** `.YYYYMMDD-HHMMSS.bak.json` へ改名。残すと前回の割当を引き継いでしまう |
| **残す** | `chunks/` | `split_audio` が**毎回消して分割し直す**。触らないほうが「分割」の所要が正しく測れる |
| **残す** | `timings.jsonl` | **追記なので消さない。**比較したいのはここ |

#### ★ `diarize/` を消す理由

**当初の要件（`transcripts/` だけ）では、この測定の目的が果たせませんでした。**

話者分離の結果は `.work_<名前>/diarize/turns.<指紋>.….json` にキャッシュされ、
2 回目以降は `load_turns` が拾って**数秒で終わります**。

**今回いちばん見たいのは話者分離の揺れ**（前回 8 分対 13 分）です。
`diarize/` を消さないと、2 回目・3 回目の話者分離が測れず、
**揺れの幅を出すという測定の目的そのものが崩れます。**

#### 動作

- 出力先のフォルダを引数で受け取る（**存在しなければエラーで止まる**。打ち間違い対策）
- **何をするかを先に全部出してから、確認を取る**（`--yes` で省ける）
- **消したあとに数え直して**、残っていれば失敗として報告する
- 2 回目に実行すると「消すものがありません」と出る
  → **消し忘れと消しすぎの両方が分かる**

**消し忘れると 2 回目以降がキャッシュから復元され、転写も話者分離もやり直しません。**
処理時間が大幅に短くなり、しかも数字は出るので気づけません。

### 1-3. ログの保存先と命名

7本のログを置く場所と、ファイル名の形式を決める。

```
測定1-1 / 測定1-2 / 測定1-3
測定2-1 / 測定2-2 / 測定2-3
測定3-1
```

**この時点では作業を止めます。** 測定の実行は人間が行います。

### 1-4.（測定後）集計と docx 作成

7本のログを受け取ってから着手する。出力先:

```
C:\dev\whosaid-editor-testdata\測定結果26-08-31-<終了時刻>.docx
```

まとめる内容:

- **揺れの幅** — 同一条件3回で、所要時間・区間数・声のまとまりの数がどれだけ動くか
- **測定1 対 測定2** — 出力先の差。**揺れの幅を超えているかどうかを明記する**
- **測定2 対 測定3** — モデルの差
- **チャンクごとの区間数の表** — 揺れがどのチャンクに出るか（§9.5.7 は「転写が苦しい
  ところだけ揺れる」としている。実測がそれと一致するか）
- **「転写の暴走」の検出** — 何回目のどのチャンクで出たか、直ったか元のままか

**差が揺れの中に埋もれている場合は、そう書くこと。** 「G ドライブのほうが速い」と
書けるのは、3回の幅を超えて速い場合だけです。

---

## 2. 人間がやる作業

### 2-1. 測定の前に確認すること（1回だけ）

> **この節は実装に合わせて書き直してあります（2026-08-31）。**
> 下の 3 点は**ピクセル座標と状態遷移で確認済み**です（ビルド 10:37:45）。
> **人が目で見る必要があるのは「窮屈に見えないか」だけ**になりました。

**確認済み（測って裏が取れているもの）:**

| | 実測 |
| --- | --- |
| モデル欄の中身 | `['標準（速い）', '高精度（遅い）']`。1号機は取得済みなので「3.0GB の取得が要ります」は付かない |
| 選択が残るか | 標準に変える → `local_model: small` が保存 → 開き直して「標準（速い）」 |
| チャンク長の欄 | `y 746-766` / 「話者を識別する」は `y 793-814` → **重ならない**。`state=normal`・7 に設定できる |

**目で見るのは 1 点だけ:**

ビルドしたアプリを起動し、**詳細設定が窮屈に見えないか**を見る。

```powershell
C:\dev\01\whosaid-editor\dist\WhosaidEditor\WhosaidEditor.exe
```

- モデル欄と「タイムスタンプを付ける」の**間隔**が詰まりすぎていないか
- チャンク長の欄と「話者を識別する」が**近すぎない**か
- 全体として崩れて見えないか

**座標が重なっていないことは測れますが、詰まって見えるかは測れません。**

**ここで問題があれば、測定に入らずに報告してください。**

### 2-2. 各回の手順

以下を **7回** 繰り返します。

**手順1 — キャッシュを消す**

PowerShell で、その回の出力先を指定して実行。

```powershell
cd C:\dev\01\whosaid-editor
.venv\Scripts\python.exe tools\clear_run_cache.py "<その回の出力先>"
```

**何をするかを先に全部出してから、確認を求めます。**

```
対象: C:\dev\whosaid-editor-testdata\測定 2（ローカル small  話者分離 ON）

  .work_01+02edited/
      消す   transcripts/  10 ファイル  720KB
      消す   diarize/  1 ファイル  41KB
      消す   inspect/  1 ファイル  72KB
      残す   timings.jsonl
      残す   chunks
  退避     01+02edited.speakers.json  306KB   ← 消さない（前回の割当を引き継がせないため）

実行しますか? [y/N]
```

`y` で実行し、**消したあとに数え直して**「消しました（キャッシュ 3 / 退避 1）。
1 回目の状態です。」と出ます。

**この表示を必ず見てください。**

- **2 回目に実行すると「消すものがありません（既に 1 回目の状態です）」**と出ます。
  → **消し忘れ**（消したつもりで消えていない）と**消しすぎ**（既に消してある回で
  もう一度実行した）の**両方がここで分かります**
- フォルダ名を打ち間違えると「フォルダがありません」で止まります
- 確認を省くなら `--yes` を付けます（**7 回とも省くのは勧めません**。
  出力先の打ち間違いに気づけなくなります）

このコマンドを飛ばすと、その回の結果は使えません。

**手順2 — アプリを起動**

> **★ 未決（2026-08-31）: 開発版と配布版のどちらで測るか。**
> **この文書は 2 通りを書いており、まだ揃っていません。**
> §2-1 は配布版（`dist\…\WhosaidEditor.exe`）を起動させ、ここは開発版です。
> **7 回とも同じほうで通してください。**混ぜると比較になりません。
>
> 違いは **cuBLAS の出所**です。開発版は `.venv` の `nvidia-cublas-cu12`、
> 配布版は `%APPDATA%\WhosaidEditor\cuda` から読みます。
> **GPU の時間を測る以上、この差が結果に出うる**ので、揃える必要があります。
>
> | | 測る意味 |
> | --- | --- |
> | 配布版 | **利用者が実際に待つ時間**。GUI に書く数字の裏付けになる |
> | 開発版 | 起動が速く、ログをそのまま拾える。**利用者の環境とは違う** |
>
> **GUI に書く数字を決めるのが目的なら配布版**です。

配布版で測る場合:

```powershell
C:\dev\01\whosaid-editor\dist\WhosaidEditor\WhosaidEditor.exe
```

開発版で測る場合:

```powershell
cd C:\dev\01\whosaid-editor
.venv\Scripts\python.exe -m src.main
```

**手順3 — 設定する**

- 音声ファイル: `01+02edited.wav`
- 出力先: その回のフォルダ（下表）
- モデル: 測定1・2 は**標準**、測定3 は**高精度**
- 話者分離: **ON**
- チャンク長: **7分**

**手順4 — 開始し、最初の数行を見る**

開始直後のログに次が出ることを確認します。

- `処理経路: ローカル(モデル small)` または `(モデル large-v3)` — **モデルが意図どおりか**
- `音声を 7 分単位で分割します...`
- `話者を分けています` の行が後で出ること

**モデルが違っていたら、そこで止めて設定し直してください。**
17分待ってから気づくと1回分が無駄になります。

**手順5 — 待つ**

標準で約17分、高精度で約32分。**放置して構いません。**
測定1 は G ドライブなので、Google ドライブの同期アイコンが動きます。
次の回を始める前に、同期が終わるのを待ってください。

**手順6 — ログを保存**

完了したらログ欄を全選択してコピーし、テキストファイルに貼って保存。
ファイル名は 1-3 で決めた形式に従います。

### 2-3. 7回の順序

| 回 | 出力先 | モデル |
| --- | --- | --- |
| 測定1-1 | `G:\マイドライブ\2026年度\アプリ開発\26-08-01逐語反訳アプリ開発\テストデータ\測定 1（1号機 small  話者分離 ON）` | 標準 |
| 測定1-2 | 同上 | 標準 |
| 測定1-3 | 同上 | 標準 |
| 測定2-1 | `C:\dev\whosaid-editor-testdata\測定 2（ローカル small  話者分離 ON）` | 標準 |
| 測定2-2 | 同上 | 標準 |
| 測定2-3 | 同上 | 標準 |
| 測定3-1 | `C:\dev\whosaid-editor-testdata\測定 3（large-v3  話者分離 ON）` | **高精度** |

**1〜3回目は出力先を変えません。** 変えるのはキャッシュを消すことだけです。

**測定3 に移るときにモデルを高精度へ変えてください。** 変え忘れると測定2 の4回目に
なります。手順4 のログで気づけます。

### 2-4. 所要時間の目安

```
測定1 (17分 × 3) ＝ 約 51分
測定2 (17分 × 3) ＝ 約 51分
測定3 (32分 × 1) ＝ 約 32分
                    ─────────
                    約 2時間15分
```

**手を動かすのは各回の最初と最後だけ**で、間は放置できます。
実際の作業時間は20分程度、残りは待ち時間です。

---

## 3. 測定中に見ておくとよいこと

**話者分離の時間。** 前回、測定1で8分、測定2で13分と5分違いました。転写の差は1分
だけだったので、ここに出力先の影響が出ている可能性があります。話者分離は転写のあとに
全長でまとめて走り、中間ファイルの読み書きが集中する処理です。3回ずつ回せば、揺れか
出力先の差かが分かります。

**「転写の暴走」の検出。** 前回、同じ設定なのに片方だけ発火しました。乱数で別の道に
入った実行が繰り返しに嵌まったためです。何回目に出るか、直るか元のままかを記録します。

**Google ドライブの同期。** 測定1 の最中に同期が走ります。次の回を始める前に完了を
待ってください。同期中に次を始めると条件が変わります。

---

## 4. 測定の結果にかかわらず、README に書くこと

**作業ファイルを Google ドライブなどの同期フォルダに置くと、音声も本文もクラウドに
アップロードされます。** このアプリはローカル完結を売りにしており、倫理審査で外部送信を
しないと約束した利用者、守秘義務のかかる音声を扱う利用者がいます。

**速さの結論がどうであれ、同期フォルダは推奨できません。** この測定は「どちらが速いか」を
測るものであって、「どちらを推奨するか」は速さでは決まりません。

---

## 5. 記録に残すこと

測定が終わったら、次を `claude/` の文書に残してください（Claude Code の作業）。

- 揺れの幅（同一条件3回の実測）
- 出力先の差が実在するかどうかの判定
- 使ったビルドの日時とコミット
- README や GUI に処理時間を書いている箇所と、実測との整合
