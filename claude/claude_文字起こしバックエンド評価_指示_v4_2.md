# 文字起こしバックエンド評価 — Claude Code 作業指示 v4.2

対象リポジトリ: `C:\dev\01\whosaid-editor`
作成日: 2026-08-30
**v1 / v2 / v3 / v4.1 を置き換える。** 旧版は削除してよい。

**v4.2 の改訂（2026-08-30・フェーズ0.5 の結果を反映）:**

- **§4-4 を訂正した。作成者（私）の誤りである。** 調査 v01 から「ゼロ長 16 件・
  重なり 23 件」を引きながら、**同文書自身の自己訂正（§4・§5）を落としていた。**
  合成データで検証したところ、これらは被覆率を壊さない。**都合のいい数字だけを
  引いた誤りである。**あわせて 3.7 Flash の条件差と、出典不明の話者数を訂正
- **§8 判定2 に「最大外れ量」と「単調性の破れ件数」を追加。** 調査 v01 §5 の発見による。
  重なり件数・ゼロ長件数を数えてもこの危険は捕まらない
- **§8 判定1 の「あの」の節を書き直した。** 誤カウントは 1 種類ではなく **3 種類**あり、
  うち 1 つは指標の定義そのものの欠陥（`FILLER_TERMS` の二重計上）
- **§7-1 の `src/lang.py` の注記を事実に合わせた。** 「検討している」ではなく**未実施**
- **§7-3 に D のモデル ID の版固定と `temperature` の固定を追加**
- **§4-3 に単語粒度の応答型が未定義である旨を追加**
- **§4-3 / §6 に D の確認順序を追加。** 実音声を送る前に、ダミー音声で逐語の挙動だけを先に見る
- §9-2 に、競合比較文書から引くのは §1 と §3 に限る旨を追加

**v4.1 の改訂（2026-08-30）:**

- v3.1 にあった実測の節（§4-4）を復元。あわせて §7-1 に 1 行注記
- **§1 に「ドキュメントと実測が食い違ったときの扱い」を追加。** 緩い方向の実測を設計の根拠にしない
- **§7-3 の B の分割数を 30 分基準で確定。** v4 にあった「§6 で確認してから決める」と「明示的に 3 分割する」の矛盾を解消
- **§7-3 に打ち切りの検出を必須要件として追加。** 黙って切られた出力を測ると、エンジンの性能ではなく打ち切りを測ることになる
- **§8 判定1 に「あの」の指示語誤カウントの切り分けを追加**
- §3 に `gemini-3.7-flash` を追加

位置づけ: **評価スパイクのみ。本線への統合はこの指示の範囲外。**

---

## v4 の改訂理由（フェーズ0 の調査結果を反映）

PR #11「ローカル転写を既定にし、話者分離を入れ、正解データで質を測る」が、
**この評価に必要な測定基盤をほぼ全て先に作っていた**ことが分かった。
人が音声を聴いて作った逐語正解（4 帯 175 区間・音声の SHA-256 で紐づく）、
測定前に固定した指標（`src/evaluate.py`）、複数エンジンを同じ物差しに載せる
集計器（`tools/report_verbatim.py`）、各エンジンを共通 SRT に落とすランナー
3 本（`tools/bench_*.py`）が既にあり、**6 系統のエンジンが同じ正解で測り終わっている。**

v3 の §7 は独立した評価スクリプト一式（`scripts/eval_stt_*.py`）を新規に作る
設計だったが、それは **A（現行 Gemini）を含む既測定の 6 エンジンと比較できない
別の物差しを作ることになる。** v4 では既存基盤への相乗りに全面的に改めた。

主な変更:

| 節 | 変更 |
| --- | --- |
| §0 | 禁止事項をブランチ名ではなく**性質**で指す。作業ブランチの起点を「PR #11 マージ後の `main`」に |
| §2 | A の実測値を明記。**PR #11 が束ねている 2 つの独立な変更**と、このスパイクの中心的な問いを追加。CER で順位をつけない旨 |
| §4-1 | 既測定の `gemini-3.5-flash` と B（`gemini-3.5-transcribe`）を混同しない旨 |
| §4-2 / §6 | **C の優先度を下げる。** B と D を先に評価し、両方が落ちた場合にのみ起こす |
| §5 | 実施済み。結果を記録として残す |
| §7 | **全面差し替え。** 新規スクリプトをやめ、`tools/bench_<backend>.py` を既存3本と同じ形で足す |
| §8 | 判定1・3 を `src/evaluate.py` の既存指標に置換。判定2 は `tools/report_timing.py` で新規に測る。判定1 に保持率の定義上の限界を明記 |
| §9-1 | **PR #11 で解決済み。マージすれば足りる** |

---

## 0. 禁止事項（最初に読むこと）

- **オープンな PR / 作業ブランチに対して、コミット・リベース・マージ・クローズを行わない。**
  対象は作業開始時に `git branch -r` と `gh pr list` で確認すること。**ブランチ名を
  この指示書に書かない** — v3 は実在しない `feature/auto-inspection` を挙げており、
  名前で指すと指示書のほうが先に古くなる。この評価はスパイクであり、
  他の作業を一切止めない前提で成立している。
- `src/transcribe.py` / `src/pipeline.py` / `src/segments.py` を**変更しない**。読むのは可。
- **PR #11 由来のファイルも変更しない**（`src/local_asr.py` / `src/diarize.py` /
  `src/evaluate.py`）。とくに `src/evaluate.py` は**指標の定義を測定前に固定した
  もの**である。**読んで再利用するのは可、値や定義を変えるのは不可。**
  出た数字を見てから正規化や語のリストを変えれば、都合の良い値を選べてしまう。
- **`tools/` 配下の既存の測定基盤を変更しない。** 変えると既に測り終えた 6 エンジンの
  値と比較できなくなる。**新しいランナーを足すのは可**（§7）。
- `audio_fingerprint` のアルゴリズムを**変更しない**（キャッシュ互換性が壊れる）。
- `requirements.txt` / `build.spec` / `installer.iss` を**変更しない**。
- パッケージを**勝手にインストール・アップグレードしない**。必要と判明したら §11 に従い
  報告して停止。**C / D は SDK が未インストールなので、必ずここで一度止まる。**
- GCP・Mistral のアカウント設定 / 課金設定 / API 有効化を**変更しない**。要否の確認までにとどめ、実行は人間が行う。
- **各フェーズの末尾で必ず停止して報告する。** 次のフェーズに自動で進まない。

作業ブランチ: **PR #11 をマージしたあとの `main`** から `spike/stt-backends` を切る。
マージ予定はない。

**起点を間違えないこと。** PR #11 が未マージの `main` には `src/evaluate.py` も
`tools/` も逐語正解も存在せず、§7 以降が丸ごと成立しない。着手時に
`git log --oneline -1` と `ls tools/report_verbatim.py` で確認する。
**未マージのままだった場合は着手せず報告して停止すること**
（`feature/local-transcribe` から切って回避しない。起点が変われば比較条件が変わる）。

---

## 1. 大原則 — 記憶ではなく一次情報

この評価の対象には、**あなたの学習データより新しい製品が含まれる**。実際にこの検討の過程で、Google 自身の Gemini チャットが「Gemini 3.5 Transcribe という製品は存在しない」と断定し、代替として旧来の実装方法を提示してきた。もっともらしい代替案が添えられていたため一見正しく読めた。

したがって以下を守ること。

- **「そのモデル / その機能は存在しない」という結論を、記憶を根拠に出さない。**
- 仕様の確認は、公式ドキュメントを直接取得するか、SDK の属性をインタプリタに問うか、API に問い合わせるかのいずれかで行う。
- この指示書に「**確認済み**」と書かれた事項は、公式ドキュメントを直接読んで確認したものである。記憶と食い違っても指示書を正とする。
- この指示書に「**要確認**」と書かれた事項は、**まだ誰も確認していない**。推測で埋めず、フェーズ 0.5 で一次情報にあたること。

### 1-1. ドキュメントと実測が食い違ったときの扱い

**§1 の「指示書を正とする」が想定しているのは「記憶 vs ドキュメント」の対立である。**
「ドキュメント vs 実測」は種類が違う。実測のほうが強い証拠だが、
**どちらの方向に食い違ったかで扱いが変わる。**

| 食い違いの方向 | 扱い |
| --- | --- |
| 実測が**厳しい**（制限が強い・上限が短い・使えるはずの組み合わせがエラーになる） | **実測を正として設計を改める。** ドキュメントに書かれていない制約が実在する |
| 実測が**緩い**（上限が長い・制限が無いように見える） | **実測を設計の根拠にしない。** ドキュメント値を上限として設計する |

**緩い方向の実測を根拠にしてはいけない理由。** ドキュメントより長く動いたことは、
その長さが**保証された**ことを意味しない。たまたま通っただけかもしれず、
条件依存かもしれず、次の更新で仕様どおりに戻るかもしれない。
緩いほうに合わせて設計すると、仕様どおりに戻った瞬間に壊れる。
**厳しいほうに合わせておけば、緩いまま動いても何も困らない。**

食い違い自体は必ず記録する（§4-4 がその例）。捨てるのではなく、
設計の根拠にしないだけである。

---

## 2. 評価候補

| 記号 | バックエンド | 位置づけ |
| --- | --- | --- |
| **A** | `gemini-2.5-flash`（現行・マルチモーダル入力＋プロンプト） | **ベースライン。** 比較の基準であり評価対象ではない。**測定済み**（§2-1） |
| **B** | `gemini-3.5-transcribe`（Gemini API / Interactions API） | 評価対象 |
| **C** | `chirp_3`（Cloud Speech-to-Text V2） | 評価対象だが**優先度は低い**（§4-2）。B と D が両方落ちた場合のみ |
| **D** | Voxtral Mini Transcribe V2（Mistral） | 評価対象 |
| **E** | faster-whisper（ローカル実行） | **評価対象外。既に本線で実装済み（PR #11）。** |

**E を候補から外している理由を誤解しないこと。** E は精度で選ばれているのではない。「ローカル完結」という製品要件で必須になっている。倫理審査・守秘義務が効く層（質的研究者、士業、社内調査）ではクラウド送信が構造的に通らないため、B / C / D がどれだけ良くても E は代替されない。**この評価の結果がどうであれ、ローカル路線を止める提案はしないこと。**

B / C / D はいずれも「AI は匿名の話者記号までしか返さない」点で既存の設計方針（実名は人が確定）と整合する。この点は三者の差にならない。

### 2-1. A は既に測定済み — 測り直さない

A（`gemini-2.5-flash`）の出力は `C:\dev\01\test-audio\bench\gemini-2.5-flash.srt` にあり、
逐語正解に対する数字が出ている（`claude/claude_ローカル化_技術方針書.md` §11.5）。

| | CER | フィラー保持 | 相づち保持 | 短発話再現 | 脱落の再現 |
|---|---:|---:|---:|---:|---:|
| **A: gemini-2.5-flash** | 29.5% | 106.0% | **130.0%** | 41.8% | **13/33** |

**相づち保持 130.0% と脱落の再現 13/33 は、いずれも 6 系統中で最良である。**
B / D はこれを超えないと採用の理由が立たない。**フェーズ2 で A を測り直さない。
人間に A の出力を依頼しない。**

### 2-2. 既測定の 6 系統（比較の土台）

`claude/claude_ローカル化_技術方針書.md` §11.5 より。B / D はこの表に行を足す形で読む。

| エンジン | CER | フィラー保持 | 相づち保持 | 短発話再現 | 脱落の再現 |
|---|---:|---:|---:|---:|---:|
| **local（small・int8・CPU・製品既定）** | **27.3%** | **9.0%** | **16.7%** | **57.3%** | **1/33** |
| large-v3-turbo | 30.7% | 17.9% | 33.3% | 45.5% | 3/33 |
| aTrain（turbo・GPU） | 34.1% | 23.9% | 36.7% | 40.9% | 2/33 |
| Notta（Privacy Mode） | 34.5% | 95.5% | 73.3% | 38.2% | 3/33 |
| **A: gemini-2.5-flash** | 29.5% | 106.0% | **130.0%** | 41.8% | **13/33** |
| AmiVoice API（-a-general） | 28.3% | **152.2%** | 70.0% | 47.3% | 5/33 |

参考（最終表に載らなかった測定済み構成）:
`small_cut`（CER 33.3% / フィラー 20.9% / 相づち 26.7% / 短発話 48.2% / 脱落 3-33）、
`small+nocond`（35.0% / 26.9% / 16.7% / 46.4% / 1-33）、
`distil-large-v3`（**英語専用で日本語を出さない。CER 103.7%・候補から除外済み**）、
`kotoba-whisper-v2.0+nocond`（44.2% / 7.5% / 10.0% / 25.5% / 1-33・不採用）、
`gemini-3.5-flash`（27.6% / 141.8% / 100.0% / 37.3% / 7-33）。

> **数値の版に注意。** PR #11 の本文と `claude/claude_正解ラベルの作り方_設計書.md` §10 は
> local の CER を **28.6%** と書いているが、これは 2026-08-16 時点の値である。
> **技術方針書 §10.3 以降と最終表 §11.5 の 27.3% が新しい。** 他の 3 指標
> （フィラー 9.0% / 相づち 16.7% / 短発話 57.3%）は変わっていない。
> 変更理由は文書に明示されていない（§11.3 で測定の土台を凍結写し
> `01+02edited.premerge.speakers.json` に切り替えた記述はあるが、この差との
> 対応づけは書かれていない）。**引用するときは 27.3% を使い、28.6% を見かけたら
> 古い版と判断すること。**

### 2-3. PR #11 は独立な 2 つの変更を束ねている — このスパイクの中心的な問い

**この節が §8 の読み方を決める。** PR #11 は 1 本の PR だが、中身は互いに独立な
2 つの変更である。

| 変更 | 実現手段 | 結果 |
| --- | --- | --- |
| **話者分離をローカルで入れる** | sherpa-onnx + pyannote-segmentation-3.0 + NeMo TitaNet | **割当操作 1219 回 → 6 回。** 第 1 候補的中率 93.2% |
| **ASR をローカル既定にする** | faster-whisper small・int8・CPU | **逐語再現が大幅に劣化。** フィラー 9.0% / 相づち 16.7% / 脱落の再現 1/33 は**いずれも 6 系統中の最下位** |

**前者の成果は sherpa-onnx によるものであり、ASR の選択とは独立である。**
話者分離が付いたから割当が 1219 回から 6 回になったのであって、
faster-whisper を選んだからではない。この 2 つを 1 つの成果として読むと、
「ローカル ASR を変えると割当の成果まで失われる」という誤った制約が生まれる。

**B と D は話者分離と単語時刻を同時に返す。** したがってこのスパイクの中心的な問いは
次の 1 文に要約できる。

> **割当の成果（1219 回 → 6 回）を保ったまま、逐語の劣化を避けられるか。**

§8 の判定は**この問いに答える形で読むこと。** 個々の指標を並べて総合点を出す作業ではない。
具体的には、B / D について次の 2 つが同時に成り立つかを見る。

1. **話者ラベルが `local + sherpa-onnx` と同等以上か**（判定3）
   — 同等でなければ、割当の成果を保てない
2. **逐語再現が local を明確に上回るか**（判定1・脱落の再現）
   — 上回らなければ、そもそも乗り換える理由がない

**どちらか一方だけでは採用にならない。** 逐語が良くても話者が崩れるなら
割当の手数が戻り、話者が良くても逐語が local 並みなら現状維持で足りる。

### 2-4. CER でこの製品の要件はほとんど測れない — 順位づけに使わない

**CER で順位をつけないこと。**

フィラー（「えー」「あの」）と相づち（「はい」「うん」）は 2〜3 文字である。
正解 2,509 字に対して、これらを全て落としても CER への寄与はごくわずかで、
**逐語録として使えるかどうかを CER はほとんど反映しない。**

実測がそれを示している。**local は CER 最良（27.3%）でありながら、
逐語再現の 3 指標すべてで最下位である**（フィラー 9.0% / 相づち 16.7% /
脱落の再現 1/33）。CER だけを見れば local が勝者になるが、
逐語録の要件では最も弱い。

したがって:

- **CER は「本文が壊れていないか」の健全性チェックとして読む。** 40% を超えるような
  値は何かが根本的におかしい（`distil-large-v3` の 103.7% は日本語を 1 文字も
  出していないことの表現だった）
- **優劣は、脱落の再現 → 話者ラベル → 相づち保持 → フィラー保持 の順で見る**
  （順序の根拠は §8-0）
- **CER が同等以下であることは、逐語保持率の改善が「本物の差」か「過剰生成の
  副作用」かを切り分けるために使う**（§8 判定1）

---

## 3. 検討して外した候補（再検討しないこと）

以下は調査済みで、今回の評価には含めない。Claude Code から「これも比較すべきでは」と提案しないこと。再検討が必要になったら人間が判断する。

| 候補 | 外した理由 |
| --- | --- |
| ElevenLabs Scribe v2 | 90+言語・単語時刻・diarization・キーターム1,000語と機能は十分だが、$0.22〜0.40/時と D の $0.18/時より高く、B / D に対する決定的な差がない。**予備候補として保留**（B / C / D が全滅した場合のみ） |
| Deepgram Nova-3 / AssemblyAI / Speechmatics | ベンチマークが英語中心で、日本語逐語の実力が不明。新規ベンダー契約の摩擦に見合わない |
| OpenAI `gpt-4o-transcribe-diarize` 系 | 直接呼ぶバックエンドとしてではなく、**出力の取り込み対象**として競合調査で整理済み。位置づけが違う |
| pyannote（community-1 等） | 話者分離専用で ASR を持たない。ローカル系と組む話であり、このスパイクの対象外 |
| Whisper API（OpenAI ホスト版） | 話者分離を持たない。ローカル faster-whisper と役割が重複 |
| distil-large-v3 | **測って外した。英語専用の蒸留モデルで、`language="ja"` を渡しても日本語を出さない**（CER 103.7%）。技術方針書 §8.3(a) |
| kotoba-whisper-v2.0-faster | **測って外した。** 全指標で small に劣り（CER 44.2%）、単語時刻を要求するとネイティブ層ごと落ちる（0xC0000005）。技術方針書 §11.1 |
| `gemini-3.7-flash`（汎用モデル） | **試して外した。言い淀みが 0 になる**ため逐語録の要件を満たさない。§4-4。<br>**この行の「時刻 472 行すべて単調・話者名も正しい」は、B とは別の音声に対する値である**（B は手元の WAV 45:11、3.7 Flash は判定講評を含む YouTube 動画）。**同一条件の比較ではないので、「3.5 transcribe より時刻と話者が良い」とは言えない。** 判定2・判定3 を読むときの参考にとどめる |

**より本質的な代替案について。** 競合調査（`claude/競合比較と汎用化の方向性.md`。
**この文書は近く `claude/` に追加される。追加後に読むこと**）では、勝ち筋は
「バックエンドを増やすこと」ではなく「**話者確定エディタの入出力をフォーマット非依存に
すること**」と結論している。RTTM / SRT / VTT / WhisperX JSON の取り込みを1本作れば、
上表の全候補と各社 SaaS のエクスポートを一度にカバーできる。バックエンドを直接叩く
実装を増やすのは、その意味で収穫逓減である。**フェーズ3の設計提案では、この観点を
必ず1項目として立てること。**

**なお、この結論は既に部分的に実証されている。** `tools/report_verbatim.py` は
`.srt` と `.speakers.json` を受け取る簡易インポータで、これ 1 本があったために
aTrain / Notta / AmiVoice / Gemini 各世代が**追加の人手ゼロ**で同じ正解に載った。
§7 で B / D も同じ経路に載る。

---

## 4. 前提となる事実

### 4-1. B: Gemini 3.5 Transcribe（確認済み・2026-08-26 公開）

> **既測定の `gemini-3.5-flash` と混同しないこと。**
> `C:\dev\01\test-audio\bench\gemini-3.5-flash.srt` は**別モデル**の出力である
> （CER 27.6% / フィラー 141.8% / 相づち 100.0% / 脱落 7/33。技術方針書 §10.3）。
> B は **`gemini-3.5-transcribe`** で、専用の Interactions API を使う転写特化モデル。
> 名前が近いため、フェーズ2 で SRT を取り違える危険がある。
> **B のランナーが出す SRT は `gemini-3.5-transcribe.srt` と命名し、
> `gemini-3.5-flash.srt` を上書きしないこと。**

出典: `https://ai.google.dev/gemini-api/docs/transcribe`、同 `docs/pricing`

| 項目 | 内容 |
| --- | --- |
| モデル名 | `gemini-3.5-transcribe`（録音済み用）。`-live` はストリーミング用で**対象外** |
| API | **Interactions API**（`client.interactions.create`）。`generateContent` ではない |
| 提供段階 | Public preview |
| `mode` | `verbatim`（既定）と `smart`。verbatim はフィラー・繰り返し・言い直し・ポーズを保持。**必ず verbatim を使う**（実測で裏づけあり → §4-4） |
| `smart` | フィラー除去・自動整形。**使わない。** timestamp / diarization と併用不可。**実測では実際の発話を黙って書き換えた** → §4-4 |
| 話者分離 | `mode.diarization_mode: "speaker"` → `spk_1`, `spk_2` … 最大8話者。**3話者以上は experimental** |
| 単語時刻 | `mode.timestamp_granularities: ["word"]` |
| 精度注記 | 単語時刻を有効にすると**全体の転写精度が劣化しうる**と公式が明記 |
| **音声長上限** | **diarization か単語時刻を有効にすると30分**（両方無効なら1時間）。実測は 40:47 まで通ったが、**§1-1 により設計はドキュメントの 30 分を上限とする** → §4-4 |
| 言語 | `language_codes: ["ja-JP"]` |
| 語彙 | `custom_vocabulary` 最大1,000語（推奨100語程度）。**ただし単語時刻とは併用不可**（ドキュメントに記載がなく実測でのみ判明。§1-1 の「厳しい方向」に該当するため**実測を正とする**） → §4-4 |
| 出力 | `interaction.output_text` に全文。注釈は `interaction.steps[].content[].annotations[]` の `type == "word_info"`（`text` / `speaker` / `start_offset` / `end_offset`） |
| 料金 | 音声入力 $0.003/分、テキスト出力 $0.002/分（実効約 $0.30/時）。**無料枠は入力が製品改善に使われるため使わない** |

```python
interaction = client.interactions.create(
    model="gemini-3.5-transcribe",
    input=[{"type": "audio", "uri": audio_file.uri, "mime_type": audio_file.mime_type}],
    generation_config={
        "transcription_config": {
            "language_codes": ["ja-JP"],
            "mode": {
                "type": "verbatim",
                "diarization_mode": "speaker",
                "timestamp_granularities": ["word"],
            },
        }
    },
)
```

**`custom_vocabulary` は渡さないこと。** 単語時刻と併用できない（実測）。
単語時刻は判定2 の中心なので、語彙ヒントのほうを諦める。

**SDK は現行のままで足りる**（フェーズ0 で確認済み）。`google-genai` 2.17.0 に
`Client.interactions` が存在する。**アップグレードしない。**

### 4-2. C: Cloud Speech-to-Text V2 / Chirp 3

> **優先度: 低（保留）。B と D を先に評価する。**
>
> C は 3 候補の中で唯一、着手前に人間の操作（gcloud 再認証）を要する。
> フェーズ0 の実測では、**gcloud のアクティブアカウントとプロジェクトが、
> 下の「確認済み」に挙げた想定と異なっており、さらに認証トークンが失効していて
> API の有効化状況すら照会できない**（`invalid_grant`）。ADC も未作成。
> `google-cloud-speech` も未インストール。
>
> **B と D の両方が §8 の判定で要件を満たさなかった場合にのみ C を起こす。**
> その時点で人間が gcloud 再認証を行い、§6 の「要確認」に着手する。
> **B か D のどちらかが要件を満たしたら、C は評価せずに終える。**
> 未確認事項を潰す作業自体に人間の手数がかかるため、必要が確定してから払う。

出典: `https://docs.cloud.google.com/speech-to-text/docs/models/chirp-3`、同 `docs/release-notes`

**確認済み:**
- Chirp 3: Transcription は **GA**（B・D と違い preview ではない）。モデル識別子 `chirp_3`。
- **Speech-to-Text API V2 専用。** v1 では使えない。
- 話者分離・自動言語判定に対応。speech adaptation によるカスタム語彙、ノイズ除去あり。
- StreamingRecognize / Recognize / BatchRecognize に対応。ただし**文字起こしと話者分離は Recognize と BatchRecognize のみ**、かつ**対応言語が限定される**と明記。
- 単語時刻は Chirp 2 で導入。`RecognitionFeatures(enable_word_time_offsets=True)`。
- 対応ロケールと機能の最新一覧は **locations API** で取得できると公式が案内。
- **既存の GCP プロジェクト（kitei-search で使用中のもの）が使える。** Cloud Storage も使用実績あり。

**要確認（C を起こす場合のみ）:**
1. **`ja-JP` が `chirp_3` の diarization 対応言語一覧に入っているか。**（× なら C を落とす）
2. `ja-JP` で単語時刻と話者分離を**同時に**有効化できるか。
3. `Recognize`（同期）の音声長上限。1分程度という記述があり、事実なら `BatchRecognize` + GCS ステージングが必須。
4. `BatchRecognize` の音声長上限。
5. リージョン（`us` / `us-central1` / `asia-northeast1` 等）での `ja-JP` 可用性。
6. **話者分離の追加課金の有無。** 公開料金ページに明示されていないという第三者指摘がある。
7. `SpeakerDiarizationConfig` の話者数 min/max 指定方法と上限。

### 4-3. D: Voxtral Mini Transcribe 2（Mistral・2026-02-04 公開）

出典: `https://docs.mistral.ai/studio/audio/speech_to_text`、`https://mistral.ai/news/voxtral-transcribe-2/`

**確認済み:**

| 項目 | 内容 |
| --- | --- |
| モデル | Voxtral Mini Transcribe V2（バッチ用）。**Voxtral Realtime は別モデルで対象外** |
| API | `/v1/audio/transcriptions` |
| 話者分離 | 対応。話者ラベルと開始・終了時刻を出力 |
| 単語時刻 | 対応（word-level timestamps） |
| 語彙 | context biasing、最大100語 |
| 言語 | 13言語。**日本語を含む**（英・中・ヒンディー・西・アラビア・仏・葡・露・独・**日**・韓・伊・蘭） |
| **音声長上限** | **1リクエストで最大3時間** |
| 料金 | **$0.003/分**（約 $0.18/時）。B より安い |
| ノイズ耐性 | 工場・コールセンター・フィールド録音などの環境で精度維持を謳う |
| 試用 | Mistral Studio の audio playground で、diarization / timestamp 粒度 / context bias をトグルして試せる。`.mp3` `.wav` `.m4a` `.flac` `.ogg`、1ファイル1GB まで、10ファイルまで |

**重要な設計上の含意:** 3時間まで1リクエストで処理できるため、**このバックエンドではチャンク分割が不要になる**。すなわち `cluster = f"{chunk}:{label}"`（チャンクをまたいだ `spk_1` が同一人物とは限らない）という前提が消える。B（30分）や C とは構造が違う。**この差はフェーズ2の判定4で必ず評価すること。**

**フェーズ0.5 で決着した項目:**

1. **逐語のモード指定は存在しない（確定）。** Mistral 自身の SDK（OpenAPI から生成）が
   リクエストの全パラメータを列挙している: `model` / `file` / `file_url` / `file_id` /
   `language` / `temperature` / `stream` / `diarize`(bool・既定 False) /
   `context_bias`(List[str]) / `timestamp_granularities`(`"segment"` | `"word"`)。
   **`verbatim` / `smart` / `clean` に相当するものは 1 つも無い。**
   **ただし「整形される」とも書かれていない。**公式ドキュメントにも発表記事にも、
   フィラー・言い淀みの扱いに関する記述が一切ない。**挙動は未決**（下の「確認の順序」）。
2. **話者数のヒント指定は存在しない（確定）。** `diarize` は boolean のみ。
   **上限値は公開ドキュメントに記載が見つからなかった**（FAQ にある可能性があるが取得できず）。
   参考: ローカル経路は自動指定にすると 246 人に割れた実測から `DEFAULT_NUM_SPEAKERS = 6`
   を明示している（`src/diarize.py`）。**D にはその手が無い。**
3. **セルフホストは不可（確定）。** Apache 2.0 のオープンウェイトは **Voxtral Realtime**
   （`voxtral-mini-transcribe-realtime-26-02`）のみで、**話者分離を持つ Transcribe 2 は
   proprietary・API 専用**である。
6. **日本語の WER 公表値は無い（確定）。** FLEURS 全体で約 4% という記載のみ。
   そもそも読み上げベンチであり会議音声の指標ではない。

**残る要確認:**

4. **データの取り扱い**（入力音声の学習利用の有無、リージョン、保持期間）。
   商用利用規約 §4.2 は「製品ごとに既定が opt-in か opt-out か」で決まる形だが、
   **Voxtral がどちらかは規約から読めない。** 保持期間とホスティング地域の記載も
   見つけられなかった。**録音は機微情報であり、ここが未確定のまま実音声を送らない**
   （CLAUDE.md）。**確認の順序は下記。**
5. **出力 JSON の構造 — 区間は確定、単語粒度は未定義（重要）。**
   SDK の型定義はこうなっている。

   ```
   TranscriptionResponse:
       model / text / usage / language
       segments: List[TranscriptionSegmentChunk] | None

   TranscriptionSegmentChunk:
       text / start / end
       type: Literal["transcription_segment"]     ← validate_const 付き
       score: float | None
       speaker_id: str | None                     ← 話者は「区間」に付く
   ```

   **`timestamp_granularities` は `"word"` を受け付けるのに、応答側に単語チャンクの
   型が無い。** 両モデルとも `extra="allow"` なので未定義フィールドとして届く可能性は
   あるが、`type` は検証付きの `Literal["transcription_segment"]` なので、
   **異なる `type` が返ると検証で落ちる作りである。**
   **判定2 は単語時刻が中心なので、これは実装前の確認事項。**
   また `speaker_id` が**区間**に付く設計なので、`diarize=True` × `word` 粒度で
   話者が単語に付くのかも不明。
7. **単語時刻と context biasing が併用できるか。** B では `custom_vocabulary` と単語時刻が
   併用不可だった（§4-4）。**D には制約の記載が無い**が、記載が無いことは
   使えることを意味しない（§1-1）。

**確認の順序 — 安いものから払う（§4-2 で C に適用した考え方を D にも適用する）**

**要確認4（データの取り扱い）を先に潰さない。** 規約確認と DPA の取得は人間の手数が
大きく、**要確認1 が × なら丸ごと無駄になる**ためである。

> **手順1（人間が行う）。** Mistral Studio の playground で、**ダミー音声**
> （人が吹き込む 30 秒程度。「えー、あのー、そうですね、ちょっと待ってください」のように
> フィラーと相づちを意図的に含める）を投げ、**逐語の挙動だけを見る。**
> **実音声は使わない。**ダミーなので、この時点ではデータ取り扱いの確認は要らない。
>
> **手順2。** フィラーが消える仕様だと分かった時点で **D を落とす。**
> 規約確認も DPA も不要になる。
>
> **手順3。** 残った場合にのみ、**要確認4 を人間が確認する。**
> それが済むまで実音声（`01+02edited.wav` および帯 WAV）を D に送らない。

---

### 4-4. 別作業での実測（2026-08-29・§4-1 と食い違う点が 2 つある）

**Google AI Studio の画面から手で** B を試したもの（コードは書いていない）。
記録は `claude/claude_Gemini系モデル_調査_v01.md`。**PR #11 のマージで `main` に入った。
この節を読む前に、その原典を必ず読むこと。**

> **英語音声（英語ディベート、45:11）での実測であり、日本語では測っていない。**
> §8 の判定1（フィラー・相づちの保持）は日本語の概念なので、そのまま持ち込めない。
> ここに書くのは、**言語によらず効く仕様の話と、時刻・話者の質**に限る。

> **v4.2 での訂正 — この節は作成者（私）が原典を偏って引用していた。**
>
> v4.1 は調査 v01 から「ゼロ長 16 件・重なり 23 件」を引きながら、
> **同文書自身の自己訂正（調査 v01 §4・§5）を落としていた。**
> 原典は「被覆率が時刻の単調性を前提にしている」という当初の記述を
> **裏取りのうえ不正確だったと撤回し、ゼロ長・重なりは被覆率を壊さないと
> 結論している。****都合のいい数字だけを引いた誤りである。**
> 下の表と §1-1 の姿勢（実測を正しく扱う）は、引用の偏りには効かない。
> **原典に自己訂正がある場合、訂正後の結論もセットで引くこと。**

#### §4-1 の記載と食い違う 2 点

| 項目 | §4-1 の記載（公式ドキュメント） | 実測 | §1-1 による扱い |
| --- | --- | --- | --- |
| 音声長上限 | diarization / 単語時刻を有効にすると **30分** | **40:47 まで転写された**（45:11 の音声。そこで「音声の長さまたは出力トークンの上限に達しました」で打ち切り） | **緩い方向** → 実測を設計の根拠にしない。**30 分を上限として設計する** |
| `custom_vocabulary` × 単語時刻 | 記載なし | **併用不可。** エラー `custom_vocabulary is incompatible with word timestamps` | **厳しい方向** → **実測を正とする。** §4-1 の表を訂正済み。`custom_vocabulary` は渡さない |

#### 分割数は変えない。ただし打ち切りの検出を必須にする

**§7-3 の「B は 30 分制限のため 3 分割」は変更しない。** §1-1 のとおり、
緩い方向の実測は設計の根拠にしない。40 分で設計して次に 30 分で切られると壊れる。

**むしろ重いのは打ち切りの理由である。**「音声の長さ**または出力トークン**の上限に
達しました」という文言は、両方の可能性を含んでいる。

> **出力トークンが縛りなら、日本語の逐語＋単語時刻は出力が膨らむため、
> 30 分でも切られうる。** 英語より早く切られてもおかしくない。

そして**黙って切られると、測定が壊れたことに気づけない。**
67 分のうち 40 分しか転写されなければ脱落の再現は当然ひどい値になり、
**それをエンジンの性能として記録してしまう。**
このため **§7-3 に打ち切りの検出を必須要件として入れてある。**

#### 判定の材料になる実測（英語・n=1）

| 観点 | 実測 |
| --- | --- |
| `verbatim` を使う方針 | **正しい。** `smart` は実際の発話を黙って訂正した。別の実測で「話者全員が talk speech と言っている」と確認済みの箇所が、`smart` では attack speech になっていた。`verbatim` だけが talk を保存した |
| 3.5 verbatim の時刻 | 127 区間中、**ゼロ長 16 件・前の区間の終わりより前に始まるもの 23 件**。<br>**ただし、この 2 つは判定の材料にならない**（下の注記） |
| 3.5 verbatim の話者ラベル（判定3 の材料） | `Speaker 0`〜`Speaker 8` の 9 種。ただし 1 つが全体の 44% を占め、分布が偏っていた |
| 参考: `gemini-3.7-flash`（汎用モデル・B ではない） | 時刻は 472 行すべて単調（逆転 0・重複 0）。話者名も正しく付いた。**ただし言い淀みが 0 になる。** 逐語録には使えない（§3 で除外済み）。<br>**B とは別の音声に対する値である**（下の注記） |

#### 注記1: ゼロ長・重なりは被覆率を壊さない（合成データで確認済み）

調査 v01 §5 が、合成データ（200 区間・45 分相当）で壊して測っている。

| 壊し方 | 結果 |
| --- | --- |
| ゼロ長 200 件 | **200/200 正しい** |
| 重なり 5 秒 / 40 秒 | どちらも **200/200 正しい** |
| ゼロ長 12.6% + 重なり 18.1%（3.5 verbatim の実測比率） | **200/200 正しい** |

窓は区間の**長さ**ではなく**時刻の点の周り**で切るため、長さ 0 でも窓幅は変わらない
（`anchor.py:47` / `:53` / `:60`）。**「ゼロ長 16・重なり 23」を根拠に B の時刻を
悪く評価してはいけない。**

**したがって「3.5 verbatim は時刻が使えない」の根拠は、打ち切りの一点である。**
調査 v01 §4 の原文:

> **決め手は 40:47 で切れること**（§1 の 30 分上限）**であって、ゼロ長・重なりでは
> ない。** …**実際の時刻がどれだけ外れているかは未測定**なので、「時刻が使えない」の
> 根拠は今のところ打ち切りの一点である。

**実際の時刻のずれ（外れ量）は誰も測っていない。** これを測るのが §8 判定2 である。

#### 注記2: 3.7 Flash は B と別の音声に対する値である

調査 v01 §0 のとおり、渡したものが違う。

| | 渡したもの | 長さ |
| --- | --- | --- |
| 3.5 smart / 3.5 verbatim | 手元の WAV（試合本編） | 45:11.87 |
| **3.7 Flash** | **YouTube 動画（判定講評を含む）** | 手元の WAV と不一致（出力 51:58） |

**同一条件の比較ではない。** v4.1 は「時刻と話者の質は 3.5 transcribe より良かった」と
書いていたが、**そう言える測定ではない。**削除した。§3 の除外理由にも同じ注記を入れてある。

#### 注記3: 削除した記述

v4.1 の話者ラベル行にあった「**話者は 8 人（発表者 6・司会・審判）**」は、
**調査 v01 に該当する記載が無く、出典を確認できなかったため削除した。**
原典にあるのは「3.5 verbatim: Speaker 0〜8 の 9 種・偏り 44%」「3.7 Flash: 名前 10 人」
だけである。**実話者数は分かっていない。**

#### この節の読み方

**`smart` の書き換えは、逐語録用途では決定的な除外理由である。** 誤変換なら読めば
気づくが、**もっともらしい別の語に静かに置き換わると気づけない。**
`reviewed`（✓）で担保しようとしている価値そのものを裏切る。

**時刻と話者の実測は、日本語での測定を省く根拠にはならない。**
英語 n=1 であり、言語によって挙動が変わりうる。判定2・判定3 の**予告**として
読み、日本語では改めて測ること。

---

## 5. フェーズ 0 — 環境と既存実装の確認（コードを書かない）

> **このフェーズは 2026-08-29 に実施済み。再実行しない。**
>
> 主な結果:
> - `google-genai` **2.17.0** に `Client.interactions` が**存在する**。B はアップグレード不要
> - Gemini の API キーは環境変数にない。`%APPDATA%\GeminiTranscriber\config.json` の
>   `api_key` にある（§7 の認証方針はこれに合わせる）
> - `google-cloud-speech` / `mistralai` はいずれも**未インストール**
> - gcloud は失効（§4-2 参照）。`MISTRAL_API_KEY` は未設定
> - `main` のキャッシュキーには**モデル識別子が入っていない**。PR #11 で解決済み（§9-1）
> - `redistribute_times()`（`transcribe.py:512`）と `merge_consecutive()`（同 603）の
>   呼び出しは `parse_segments()` の 1 箇所のみ。**どちらも「`[MM:SS] 【A】 本文`」という
>   行テキストを経由した入力**を前提としており、時刻は秒に丸められ単語時刻を持たない。
>   B / C / D の単語時刻をこの経路に載せると、実測時刻を捨てて文字数按分で作り直すことになる
> - チャンク長は GUI の Spinbox のみで決まる（既定 15 分・1〜30 分）。
>   `split_audio()`（`audio.py:216`）は**無条件に 16kHz モノラル 64kbps AAC へ
>   再エンコードする** — 分割を迂回すると音質条件が揃わなくなる点に注意
>   （§7-3 の帯 WAV 共用で回避される）
> - `.venv` は Python 3.13.3 だが CLAUDE.md と CI は 3.12。今回の評価には影響しない
>
> 以下は記録として残す。**起点ブランチを変えた場合のみ、1 と 6 を再確認すること。**

ファイルを1つも作らない。調べて報告するだけ。

1. `git status` / 現在のブランチ / `main` が origin と同期しているか。差分があれば報告して**停止**。
2. `.venv` を有効化し `pip show google-genai` でバージョン確認。
   `python -c "from google.genai import client; print('interactions' in dir(client.Client))"` で Interactions API の有無を確認。
   **無い場合は必要バージョンを報告して停止。アップグレードしない。**
   （`genai.Client()` の実インスタンス化は API キーが要るので、クラス属性で見ること）
3. `pip show google-cloud-speech` / `pip show mistralai` の有無を確認（**インストールしない**）。
4. `gcloud` の認証済みアカウントとアクティブプロジェクト、Speech-to-Text API の有効化状況を確認（**有効化しない**）。
5. Mistral の API キーが環境変数にあるか確認（**取得・設定はしない**。無ければその事実を報告）。
6. キャッシュキーの生成箇所を特定し、ファイル名・関数名・行番号と、キーに含まれる要素を列挙。
7. `redistribute_times()` と `merge_consecutive()` の定義位置・呼び出し箇所、およびそれぞれが前提にしている入力形式を報告。
8. **チャンク分割の実装箇所を特定し、チャンク長がどこで決まるかを報告。** D はチャンク不要になる可能性があるため、分割を迂回できる構造かを見る。

→ **停止。報告のみ。**

---

## 6. フェーズ 0.5 — 仕様の一次情報確認（コードを書かない）

> **このフェーズは 2026-08-30 に実施済み。** 結果は §4-3 と下の表に反映してある。
> **再実行しない。**残っているのは次の 2 つで、**どちらも人間が行う。**
>
> 1. **D の逐語の挙動**を Mistral Studio の playground で見る（**ダミー音声**を使う。
>    §4-3「確認の順序」）。**ここで D が落ちる可能性がある。**
> 2. 1 が通った場合にのみ、**D のデータ取り扱い**（§4-3 要確認4）を確認する
>
> C は保留のまま（§4-2）。**gcloud には触れない。**

**§4-3（D）の「要確認」項目を潰す。** 公式ドキュメントを直接取得して読む。
**ドキュメントが動的タブで取得できない場合は、Mistral 自身の SDK
（`github.com/mistralai/client-python` の `models/` 配下、OpenAPI から生成）を
根拠にしてよい。**§1 の「SDK の属性をインタプリタに問う」と同じ性質の一次情報である。
**取得できなかった項目は、推測で埋めず「取得できず」と書く。**

**§4-2（C）の「要確認」項目は、この時点では潰さない。** §4-2 の枠に書いたとおり
C は保留であり、locations API への問い合わせには gcloud の再認証（人間の操作）が要る。
**B と D が両方とも落ちたときに、改めてこのフェーズの C の部分だけを実行する。**
その際は **ドキュメントの散文ではなく API の返り値**を根拠にすること。

D の要確認のうち **項目1（日本語で verbatim 相当の出力になるか）が最重要**である。
B の `verbatim` に相当するモード切替が公開ドキュメントに見当たらない。

**B については、追加で 1 点だけ確認する。**
§4-4 の打ち切りが「音声長」と「出力トークン」のどちらによるものか、
公式ドキュメントに出力トークンの上限に関する記載があるかを調べる。
**分割数は 30 分基準で確定済みなので、この確認で分割数は変えない**（§1-1）。
確認の目的は、§7-3 の打ち切り検出の閾値を妥当に決めることにある。

結果を次の形の比較表にまとめて報告する。**列順は B / D / C**（優先度の順）。
**以下は 2026-08-30 に埋めた結果である。**

| 観点 | B: gemini-3.5-transcribe | D: Voxtral Mini Transcribe 2 | C: chirp_3（保留） |
| --- | --- | --- | --- |
| 提供段階 | Public preview | **GA / Premier**（公開 2026-02-04） | GA |
| モデル ID | `gemini-3.5-transcribe` | **表記が 3 通りに割れている**（§7-3） | `chirp_3` |
| 日本語 + 話者分離 | 対応 | 対応（13 言語に日本語を含む） | 保留 |
| 日本語 + 単語時刻 | 対応 | 対応（`timestamp_granularities: ["word"]`） | 保留 |
| 両方同時 | 対応 | **パラメータ上は可。ただし応答型が未定義**（§4-3 要確認5） | 保留 |
| **1リクエストの音声長上限（設計値）** | **30分** | **3時間** | 保留 |
| **チャンク分割の要否（67分音源）** | **必要（3分割）** | **不要** | 保留 |
| **出力トークン上限の記載** | **公式に記載なし** | 記載なし | 保留 |
| **逐語（フィラー保持）の明示的な指定** | あり（`verbatim`） | **存在しない（SDK で確定）。整形されるとも書かれていない** | 保留 |
| 単語時刻と語彙ヒントの併用 | **不可（実測のみ）** | 制約の記載なし | 保留 |
| **話者数上限 / ヒント指定** | 8（3以上 experimental） | **上限の記載なし。ヒント用パラメータは存在しない** | 保留 |
| 語彙ヒント | 1,000語（ただし単語時刻と併用不可） | `context_bias` 100語 | 保留 |
| 出力が決定的か | 未確認 | **`temperature` あり。固定すること**（§7-3） | 保留 |
| 料金（1時間あたり） | 約 $0.30 | **$0.18**（$0.003/分） | 保留 |
| データの学習利用 | 有償枠では無し | **確定できず**（§4-3 要確認4） | 保留 |
| セルフホスト | 不可 | **不可（確定）** | 不可 |
| 日本語 WER の公表値 | なし | **なし**（FLEURS 全体 約4% のみ） | 保留 |
| 認証 | API キー | API キー | 保留 |

**打ち切り条件:**
- **D の「逐語の指定」が存在せず、出力が整形される仕様だと判明した場合は、その時点で報告して停止すること。** 逐語録が要件である以上、整形されたら使えない。
  > **2026-08-30 時点の判定: 発火しない。** 前半（指定が存在しない）は確定したが、
  > **後半（整形される仕様）は公式のどこにも書かれていない。**条件は「かつ」なので
  > 満たさない。**ただし「モードで逐語性を担保することはできない」ことは確定した。**
  > 担保できるかどうかは §4-3「確認の順序」の手順1（ダミー音声）で決める。
- **B と D の両方が落ちた場合にのみ、C の評価を人間に提案する。**
  その提案には gcloud 再認証が必要である旨を必ず書く。
- **C を起こした場合、その項目1（`ja-JP` が `chirp_3` の diarization 対応言語一覧に
  入っているか）が × なら、そこで C を落として終える。**

→ **停止。報告のみ。**

---

## 7. フェーズ 1 — 既存の測定基盤にランナーを足す（実行はしない）

**新しい評価スクリプト一式を作らない。** `scripts/eval_stt_*.py` と
`words.tsv` / `segments.tsv` は v3 の設計だったが、**それは A（現行 Gemini）を
含む既測定の 6 エンジンと比較できない別の物差しを作ることになる。**
§8 のベースラインが A である以上、これは目的に反する。

### 7-1. 既にあるもの（読んでから書く）

| 場所 | 役割 |
|---|---|
| `src/evaluate.py` | **指標の定義。測定前に固定済み。このスパイクでは変えない。** `cer` / `retention_rate` / `cluster_purity` / `purity_chance_level` / `stratified_halfwidth` / `verdict`<br>**注（v4.2 で事実に合わせた）: `src/lang.py` 経由での指標の言語分岐は、PR #11 では実施されていない。** `src/lang.py` は存在する（`Matching` / `Speech` / `TextJoin` / `Labels` / `Asr` / `Prompt`）が、**CER 用の正規化も語リストも持っておらず、`src/evaluate.py` は `lang` を import していない。** `_CER_STRIP` と `FILLER_TERMS` / `BACKCHANNEL_TERMS` は `evaluate.py` に直値のままで、`count_terms()` も**未変更**である。<br>英語側で挙がっていた 3 点（`_CER_STRIP` に半角スペースが入っている・ASCII の句読点が無い・`count_terms()` が部分一致で `um` が `summary` に当たる）は、**検討されただけで未実施。****このスパイクからは触らないこと。**<br>**`count_terms()` の部分一致は日本語にも効いており、実データで確認済みである。§8 判定1 を参照。** |
| `tools/report_verbatim.py` | 複数エンジンを同じ正解に載せる集計器。**`.srt` と `.speakers.json` を受ける** |
| `tools/bench_models.py` | ローカル各モデルのランナー。**帯 WAV（`band.<帯>.wav`）を切り出すのもここ** |
| `tools/bench_cloud.py` | Gemini のランナー（113 行）。**B のランナーはこれを写す** |
| `tools/bench_amivoice.py` | 外部 REST API のランナー（186 行）。**D のランナーはこれを写す** |
| `C:\dev\01\test-audio\truth\verbatim.*.json` | 逐語正解 4 帯 175 区間。音声の SHA-256 で紐づく |
| `C:\dev\01\test-audio\truth\labels_blind.r1.json` | 段階1 の話者ラベル正解 300 件 |
| `C:\dev\01\test-audio\bench\band.*.wav` | 正解のある 4 帯の切り出し音声（各 2 分） |

**`src/` を import してよい。** v3 は「本線を汚さないため import しない」としたが、
`src/evaluate.py` は画面もモデルも要らない純粋関数だけで（ファイル冒頭に明記）、
import しても本線の挙動に一切影響しない。既存の `tools/bench_*.py` は実際に
`sys.path.insert` して `src/` を import している。**同じ形に揃えること。**

### 7-2. 作るもの

```
tools/bench_gemini35.py    — B 用。tools/bench_cloud.py を写す
tools/bench_voxtral.py     — D 用。tools/bench_amivoice.py を写す
tools/bench_chirp3.py      — C 用。B と D が両方落ちた場合のみ（§4-2）
tools/report_timing.py     — §8 判定2 と判定1 の内訳。唯一の新規設計
```

`eval_stt_common.py` に相当するものは**作らない。** 正規化は
`report_verbatim.py:61` の `load_engine()` が、集計は同スクリプトが既に担う。

### 7-3. ランナーの共通要件

- **出力は既存の SRT**（`bench/<name>.srt`）。時刻は**元音声の絶対時刻**にする
  （帯の開始秒を足す）。相対のままだとどの帯にも当たらない
- **B の SRT は `gemini-3.5-transcribe.srt` と命名する。**
  既存の `gemini-3.5-flash.srt` を上書きしない（§4-1）
- **生レスポンスを丸ごと `bench/<name>.raw.<帯>.json` に保存する。**
  課金が発生する以上、解析のたびに API を叩き直すのは無駄。1回投げて保存し、
  あとは何度でも解析し直せるようにする。`bench_amivoice.py` が既にこの形
- **入力は 2 系統を用意する。**
  - **帯 4 本**（`band.*.wav`・各 2 分・合計 8 分）→ 判定1・判定2 に使う。
    既存 6 エンジンと同一条件になり、B の 30 分制限にも当たらない
  - **全長**（`01+02edited.wav`・67 分 16 秒）→ 判定3・判定4 に使う。
    D は 1 リクエスト、**B は 30 分を上限として 3 分割**、C は §6 の確認しだい。
    **判定4（チャンク分割の要否がもたらす差）は全長でしか観測できない。**
    費用は B が約 $0.34、D が約 $0.20 で、両方走らせても差し支えない
- **B の分割数は 30 分基準の 3 分割で確定する。**（§1-1・§4-4）
  実測では 40:47 まで通っており公式の 30 分と食い違うが、**緩い方向の実測は
  設計の根拠にしない。** 40 分で設計して次に 30 分で切られると壊れる。
  **明示的に 3 分割する経路を書く**（暗黙に分けない・分割数をログに出す）
- 音声長を先に測り、**そのバックエンドの設計上限を超えていたらエラーで停止。
  勝手に分割しない。**

#### D のモデル ID は版を固定した表記を使う

**Mistral 自身のドキュメント内で表記が 3 通りに割れている。**

| 出典 | 表記 |
| --- | --- |
| コード例（offline transcription） | `voxtral-mini-latest` |
| モデル一覧 | `voxtral-mini-transcribe-26-02` |
| モデルカード | `voxtral-mini-transcribe-2` |

**`-latest` はエイリアスである。使わない。** キャッシュキーにモデル識別子が入る設計
（§9-1）と、測定の再現性の両面から、**版を固定した ID を使う。**
エイリアスのまま測ると、Mistral 側が中身を差し替えたときに**同じ鍵で別物の転写が
返る**——PR #11 が 4 回起こした事故と同じ形になる。

**どの表記が実際に通るかは API に問い合わせて確認すること**（モデル一覧の取得で足りる）。
**推測で選ばない。**確認した結果を `meta` とログに残す。

#### `temperature` を明示的に固定する

**D の転写 API には `temperature` がある**（SDK の `AudioTranscriptionRequest` で確認）。
転写に温度があるということは、**出力が決定的でない可能性がある**ということである。

**同じ音声を投げ直して違う結果が出ると、キャッシュも比較も成立しない。**
生レスポンスを保存して解析し直す設計（上記）も、投げ直しが再現しないなら意味が薄れる。
**明示的に固定し、その値をログと `meta` に残すこと。**既定値まかせにしない。

B にも同種の設定があれば同じ扱いにする。**「既定だから書かない」をしない。**

#### 打ち切りの検出（必須）

- **出力が入力音声の全長をカバーしているか検証し、していなければエラーで停止する。**
  各リクエストについて、最終区間の終了時刻と入力音声の長さを比べ、
  **カバー率が 95% を下回ったら「打ち切られた可能性がある」として中断する。**
  カバー率・最終区間の終了時刻・入力音声長を必ずログに出す。
- **理由。** B は英語 45:11 の音声で 40:47 で打ち切られた実績があり、
  そのメッセージは「音声の長さ**または出力トークン**の上限に達しました」だった（§4-4）。
  **出力トークンが縛りなら、日本語の逐語＋単語時刻は出力が膨らむため 30 分でも
  切られうる。** 英語より早く切られてもおかしくない。
- **黙って切られた出力を測ると、エンジンの性能ではなく打ち切りを測ることになる。**
  67 分のうち 40 分しか転写されなければ脱落の再現は当然ひどい値になり、
  それをエンジンの欠陥として記録してしまう。**この誤りは数字を見ても分からない。**
  実際に落ちている発話が多いのか、後半が丸ごと無いのか、区別がつかないためである。
- **帯 4 本（各 2 分）でも同じ検証を行う。** 短いから安全とは限らず、
  検証を条件分岐にすると片方だけ検証されない状態が生まれる。
- 打ち切りが検出された場合は**リトライしない。** 中断して報告する
  （分割を細かくするかどうかは人間が決める）。

#### その他

- API エラーは既存の `classify_api_error()` と同じ思想で扱う。**即座に中断して原因と
  対処を表示。リトライで誤魔化さない。**（v2.0.2 で 429 を3回ずつ再試行して中途半端な
  結果を作った件の再発防止）
- **自動実行・バッチ処理・ループを入れない。** 実行は人間が行う
- **音声の前処理を勝手に足さない。** 帯 WAV をそのまま渡す。既存 6 エンジンと
  同じ入力でなければ比較にならない
- **B に `custom_vocabulary` を渡さない。** 単語時刻と併用不可（§4-1・§4-4）。
  単語時刻は判定2 の中心なので、語彙ヒントのほうを諦める
- 認証情報:
  - **B は `%APPDATA%\GeminiTranscriber\config.json` の `api_key` を読む**
    （`bench_cloud.py` と同じ経路。環境変数にはない。新しい設定ファイルは作らない）
  - **D は環境変数 `MISTRAL_API_KEY` からのみ読む。引数やファイルには書かない**
    （履歴に残るため。`bench_amivoice.py` と同じ方針）
  - C は gcloud ADC
- **録音は外部に送られる。** B / C / D いずれも測定用であり、製品からは呼ばない。
  ヘッダにその旨を書く（`bench_cloud.py` / `bench_amivoice.py` に前例あり）。
  D は**入力音声の学習利用の有無・保持期間を §6 で確認してから**実行する

### 7-4. `tools/report_timing.py` の要件（新規設計）

**このファイルは 2 つの役割を持つ。** 名前は時刻（timing）だが、
**§8 判定1 の「保持率の超過分の内訳」も同じファイルで算出する。**
`src/evaluate.py` を変更できない以上どこかに置く必要があり、
**ファイルを増やすよりよい**という判断である。
**冒頭にこの旨を明記すること** — 名前が中身より狭いことを、
読む人が最初の 5 行で分かるようにする。

入力は `report_verbatim.py:61` の `load_engine()` を再利用して読む
（同じ SRT を別の読み方で解釈しない）。
**指標の定義を、実データを見る前にこのファイルに固定してから測ること。**

**役割1: 時刻の精度（判定2）**
- 隣接区間の重なり件数
- 1秒未満の極端に短い区間の件数
- 文字数と区間長の相関
- 音声全体の長さと最終単語の終了時刻のずれ（進行性ドリフト）
- **最大外れ量**
- **単調性の破れ件数**（§8 判定2 の理由を読んでから実装すること。
  **ここが「静かに誤る」経路を捕まえる唯一の指標である**）

**役割2: 保持率の超過分の内訳と、語の誤カウントの切り分け（判定1）**
- 100% を超えたエンジンについて、超過分が「正解と一致するもの」か
  「正解に無い挿入」かを分けて数える。定義は §8 判定1 に置く。
- **あわせて語の誤カウントを 3 種類に分けて数える**（§8 判定1）。
  `count_terms()` は形態素解析をしない部分一致の総和なので、
  **(1)「あのー」の二重計上**（`FILLER_TERMS` に `"あのー"` と `"あの"` が両方ある。
  確定 3 件）、**(2)「あの」の指示語**（5〜14 件）、**(3)「ちょっと」の副詞**（約 6 件）
  が混ざっている。分母は 21〜34% 過大である。
- `BACKCHANNEL_TERMS` も同じ検査にかけること
  （`"ええ"` は `"ええと"` の一部でもある。実測で `ええと、そうですね` は
  相づち 2 件として数えられた）。

→ **作成後、実行せずに停止。** ファイル内容を提示して確認を取る。

---

## 8. フェーズ 2 — 実測結果の分析

人間が §7-3 のランナーを実行し、`bench/*.srt` を置く。**A（gemini-2.5-flash）の
出力は既に `bench/gemini-2.5-flash.srt` にあるので、人間に依頼しない。**
音源は 67 分 16 秒（`01+02edited.wav`。v3 の「52分」は誤り）。
判定1・2 は帯 4 本、判定3・4 は全長で見る（§7-3）。

**分析の前に、全てのランナー出力についてカバー率を確認すること**（§7-3 の打ち切り検出）。
**カバー率が 95% を下回っている出力は分析に含めない。** 打ち切られた出力を混ぜると、
打ち切りをエンジンの性能として記録することになる。

`claude/claude_stt_バックエンド評価結果.md` を作成する。判定は次の4点。**各項目に ◯ / △ / × と、根拠となる数字を必ず添えること。数字のない所感は書かない。**

### 8-0. 読み方の前提（先に読むこと）

**この判定は §2-3 の問いに答える形で読む。**

> 割当の成果（1219 回 → 6 回）を保ったまま、逐語の劣化を避けられるか。

**判定3（話者ラベル）と 判定1（逐語）の両方が成り立って初めて採用の候補になる。**
どちらか一方だけでは採用にならない。逐語が良くても話者が崩れるなら割当の手数が戻り、
話者が良くても逐語が local 並みなら現状維持で足りる。

**指標の優先順位は次のとおり。**

1. **脱落の再現** — 区間そのものが無いと割り当てる先が無い。本文は人が打てば直るが、
   区間が無いと隣接区間の分割から始める必要があり、人の手数が最も大きい
   （技術方針書 §8.4）
2. **話者ラベル** — 崩れると割当の成果が失われる
3. **相づち保持** — 同意の意思表示が記録から消える
4. **フィラー保持**
5. **CER** — **順位づけには使わない**（§2-4）。健全性チェックと、
   保持率の改善が本物か過剰生成の副作用かの切り分けにのみ使う

**合格線は既に決まっている。** 技術方針書 §11.7 が
「以後の新エンジンは、**脱落の再現で 13/33 を超えるか**だけを見て判断する」と
宣言している（13/33 は A＝`gemini-2.5-flash` の値）。B / D はまずこの線で判定する。
**超えないなら、他の指標がどれだけ良くても採用の理由にならない。**

### 判定1: フィラー・相づちの保持 ← `evaluate.retention_rate()` で測る

`tools/report_verbatim.py` をそのまま実行し、既測定の 6 エンジンと同じ表に並べる。

```
python tools\report_verbatim.py C:\dev\01\test-audio\truth ^
  local=... turbo=... atrain=... notta=... gemini25=... amivoice=... ^
  gemini35t=... voxtral=...
```

**出現件数を数えてはいけない。** 正解に無いフィラーを出したエンジンが高く出る。

#### この指標の定義上の限界（結果を書く前に理解すること）

`retention_rate` は**正解のフィラー数を分母とする単純な比であり、位置の一致を見ていない。**
したがって次の病理を検出できない。

> **実在するフィラーを全て落とし、別の場所に同数を捏造しても 100% になる。**

**逐語録では、捏造は脱落より重い欠陥である。** 落ちた発話は「記録が不完全」だが、
無かった発話が入るのは「記録が虚偽」であり、`reviewed`（✓）で担保しようとしている
価値そのものを裏切る。しかも人が聴いて直すとき、脱落は「足りない」と気づけるが、
**捏造はもっともらしく読めてしまうため気づけない。**

**したがって 100% を超えたエンジンについては、超過分の内訳を必ず出すこと。**

| 内訳 | 意味 |
| --- | --- |
| **正解と一致するもの** | 正解の位置に、正解と同じ語が出ている。本物の逐語性 |
| **正解に無い挿入** | 正解に対応する語が無い位置に出ている。**捏造** |

内訳は **`tools/report_timing.py` で算出する**（§7-4 の役割2）。
**`src/evaluate.py` は変更しない。** 指標の定義を測定前に固定した意味が失われる。

**実測で 100% を超えているのは AmiVoice（152.2%）・gemini-2.5-flash（106.0%）・
gemini-3.5-flash（141.8%）である**（Notta の 95.5% も高い）。
**B / D がこの帯に入った場合、内訳を出すまで「逐語性が高い」と書かないこと。**

#### 語の誤カウント — **3 種類ある**（v4.2 で書き直した）

**`count_terms()` は形態素解析をせず、`body.count(t)` の総和である**（`evaluate.py:243`）。
語リストは `evaluate.py:200-203` に直値で置かれている。

```
FILLER_TERMS = ("えー", "えっと", "ええと", "あのー", "あの", "まあ", "うーん",
                "んー", "そのー", "なんか", "ちょっと")
```

**フェーズ0.5 で正解データ（4 帯・2,689 字・フィラー合計 67）を読んで確認した結果、
誤カウントは 3 種類ある。** v4.1 は 1 種類しか挙げていなかった。

**(1) 「あのー」の二重計上 — 指標の定義そのものの欠陥。確定 3 件**

`FILLER_TERMS` に **`"あのー"` と `"あの"` が両方入っている。**部分一致の総和なので、
「あのー」は**必ず 2 回**数えられる。実際に `count_terms("あのー、そうですね", FILLER_TERMS)`
は **2** を返す。正解データ中の「あのー」は 3 件なので、**確定で 3 件の過大計上。**
これは音声の性質でも言語の難しさでもなく、**語リストの作り方の誤りである。**

**(2) 「あの」の指示語 — 5〜14 件**

正解データ中の「あの」は 23 件。内訳は `あのー` 3（上記(1)）／`あの、`（読点つき）6 ／
**直後が名詞・動詞のもの 14**。後者のうち `あの子ども` `あの学校を` `あの経理者`
`あの祖父が` `あの高校に` は指示語と読める。人名が続くもの（`あの阿部俊…` 等）は判別困難、
残りは読点が落ちたフィラーとも読める。**確定はできないので 5〜14 の幅で扱う。**

**(3) 「ちょっと」の副詞 — 約 6 件**

正解データ中 7 件のうち、「ちょっと長くなった」「ちょっと抵抗感がある」
「ちょっと違うと思う」「ちょっと辞意をもらう」など、**大半が副詞「少し」である。**
フィラーではない。

なお `なんか`（11 件）と `まあ`（7 件）は、文脈を見た限り概ね談話標識として真正だった。

#### 分母が 21〜34% 過大である — 絶対値を語るときの注意

**合計すると 14〜23 件 / 67 件、すなわち 21〜34% の過大計上である**
（二重計上 3 ＋ 指示語 5〜14 ＋ 副詞 約 6）。

**これは分母（正解側）の問題なので、全エンジンに等しく効く。**
したがって **順位は動きにくい。**しかし——

> **保持率の絶対値は、全エンジンで過小に出ている。**
> 分母が過大なら率は過小になる。「local のフィラー保持 9.0%」は、
> 分母が 50 なら **12.0% 相当**である。

**§8-0 の合格線を絶対値で語るときは、この点に注意すること。**
「フィラー保持が一桁台だから逐語録に使えない」という読み方は、分母の過大分だけ
厳しすぎる。**比較（どちらが上か）には使えるが、水準の判定には補正が要る。**

同じことが 100% 超の解釈にも効く。保持率が高い理由が過剰生成なのか誤カウントなのかは、
**内訳を出すまで区別がつかない。既測定 6 エンジンの 100% 超も、一部は誤カウントで
説明がつく可能性がある。**

#### 切り分けの手順

内訳を出す際、**(1)(2)(3) をそれぞれ分けて数えること**（`tools/report_timing.py` の役割2）。
判定に使うのは分離後の数である。

**`src/evaluate.py` は変更しない。** 既測定 6 エンジンの数値との比較可能性が失われる。
切り分けは内訳側だけで行い、**元の `retention_rate` の値も併記する**
（既存の表に並べるのは元の値、解釈に使うのは分離後の値）。

**同様の誤カウントが他の語にも無いか、正解データの該当箇所を目視で確認すること。**
確認した語と結果をレポートに列挙する。`BACKCHANNEL_TERMS`（`はい` `ええ` `うん`
`なるほど` `そうですね` `わかりました`）も同じ検査にかけること——
`ええ` は `ええと` の一部でもあり、**フィラー側と相づち側で同じ文字列を二重に
数えている可能性がある。**

#### CER との併読

過剰生成は CER を悪化させる。
**フィラー保持が高く CER も同等以下なら本物の差、CER が悪化しているなら交換である。**
`condition_on_previous_text` を切ると保持率は 3 倍になるが CER が悪化する
（技術方針書 §8.3(c)「交換であって改善ではない」）。この読み方を踏襲する。
**ただし CER で順位はつけない**（§2-4）。

### 判定2: 時刻の精度 ← `redistribute_times()` の要否を決める判定

**`tools/report_timing.py` で新規に測る。** `src/evaluate.py` に該当指標が無く、
ここだけが v3 から引き継ぐ新規作業である。測る前に定義を固定すること。

- 隣接区間の重なり件数
- 1秒未満の極端に短い区間の件数（A では「37文字が1秒」という機械的な割り振りが起きていた）
- 文字数と区間長の相関（機械的に均等割りされていないか）
- 音声全体の長さと最終単語の終了時刻のずれ（進行性ドリフトの有無）
- **最大外れ量**（v4.2 で追加）
- **単調性の破れ件数**（v4.2 で追加）
- **比較対象に A（gemini-2.5-flash）と local を必ず入れる。** 「B の時刻が良い」は
  相対的な主張であり、`redistribute_times()` が現に相手にしている出力と
  並べないと要否を判断できない

#### 最大外れ量と単調性の破れを足す理由（v4.2）

**重なり件数とゼロ長件数を数えても、本当に危険な壊れ方は捕まらない。**

調査 v01 §5 が合成データ（200 区間・45 分相当）で確かめている。

| 壊し方 | 結果 |
| --- | --- |
| ゼロ長 200 件 | **200/200 正しい** |
| 重なり 5 秒 / 40 秒 | どちらも **200/200 正しい** |
| ゼロ長 12.6% + 重なり 18.1% | **200/200 正しい** |
| 時刻が全部 0 | 197/200 が誤り。ただし**全件が被覆率の壁(0.60)に落ちる** |
| 完全な逆転（降順） | 199/200 が**照合不能** |
| **1 区間だけ 5 分先 × 本文が互いに似ている** | **誤りなのに壁を越えた 128 件（被覆 0.93〜0.97）** |

**ゼロ長と重なりは、それ自体では何も壊さない。** 窓は区間の長さではなく時刻の点の
周りで切るため（`anchor.py:47` / `:53` / `:60`）、長さ 0 でも窓幅は変わらない。

**効くのは 2 つだけである。**

1. **窓幅を超えて外れること**（±30 秒、拾い直し ±120 秒）。
   → だから**最大外れ量**を測る。ただしこの場合は照合不能か被覆率の壁で落ちるので、
   **黙っては通らない。**
2. **単調性が破れ、かつ本文が互いに似ていること。**
   → だから**単調性の破れ件数**を測る。**こちらが本当に危険である。**

**2 が危険な理由。** 単調掃引 `floor` は次の区間を直前の一致より後ろからしか探さない
（`anchor.py:270` / `:283`）。1 区間が先へ飛ぶと `floor` がそこまで進み、後続が軒並み
範囲外になる。そこで**近くの似た区間に高い被覆率で乗ってしまう。**合成データでは
**128 件が被覆 0.93〜0.97 で誤った時刻を返した。**被覆率の壁（0.60・`inspection.py:39`）を
悠々と越えるので、**検査を素通りする。**

> **会議の音声は本文が似る。**「はい」「そうですね」「よろしくお願いします」が
> 繰り返し現れる。**この条件は実際に起こりうる。**

本文が十分に違う場合は 0 件で、代わりに照合不能として落ちた。
**つまり「静かに誤る」かどうかは、時刻の壊れ方と本文の似方の掛け算で決まる。**

**この裏取りは合成データである。実音声では測っていない。** 3.5 verbatim の実際の時刻が
窓幅を超えて外れているかは**誰も測っていない**（§4-4 注記1）。**それを測るのがこの判定である。**

**参考として §4-4 の英語実測がある**（3.5 verbatim は 127 区間中ゼロ長 16 件・
逆転 23 件）。**ただし英語 n=1 であり、日本語の測定を省く根拠にはならない。**
日本語で同じ傾向が出たら、英語実測と一致した事実を書く。出なかったら、
言語で挙動が変わることを書く。どちらも意味のある結果である。

### 判定3: 話者ラベル ← `evaluate.cluster_purity()` と `purity_chance_level()` で測る

正解は段階1（`truth/labels_blind.r1.json`・300 件・人が白紙画面で音声を聴いて割当）。

**異なり数の照合で判定しない。** 全区間を 1 話者にしたエンジンが有利になる。
`purity_chance_level()` で**偶然一致を差し引いた純度**を出し、
`stratified_halfwidth()` で誤差幅を添える。

**比較対象は `local + sherpa-onnx`（第 1 候補的中率 93.2%）である。**
§2-3 のとおり、この 93.2% は sherpa-onnx の成果であって faster-whisper の成果ではない。
**B / D がこれと同等以上でなければ、割当の成果（1219 回 → 6 回）を保てない。**

**この判定には全長の出力が要る。** 段階1 の 300 件は 67 分全体から層別抽出されており、
帯 4 本（8 分）に落ちる件数はごく少ない。**帯だけで測ると分母が立たない。**
帯の出力しか取れなかった場合は、**その事実と件数を書いたうえで「測定不能」とする。**
少ない標本で純度を出して比較しない。

**打ち切りがあった場合も同様に「測定不能」とする**（§7-3）。
後半が欠けた出力で純度を出すと、欠けた区間が丸ごと不一致として数えられる。

3話者以上での崩れ方は、B の公式注記（3話者以上は experimental）に対応する
観察項目として残す。ラベルが入れ替わっている箇所の件数も併せて報告する。
**§4-4 の英語実測では 9 種のうち 1 つが全体の 44% を占める偏りが出ている。**
日本語で同じ偏りが出るかを見る。

### 判定4: チャンク分割の要否がもたらす差（D 固有）

D が全長を1回で処理できた場合、チャンク境界をまたぐ話者同一性の問題が発生しないはずである。B / C のチャンク境界付近で発生している話者の不連続と比較し、**この構造的な差が実測で確認できるかを検証する。** できるなら、これは料金や WER より重い判断材料になる。

**この判定は全長の出力でしか観測できない**（§7-3）。帯 4 本では B も D も
1 リクエストに収まってしまい、構造差が現れない。

**B のチャンク境界は 30 分・60 分の位置にある**（3 分割）。
その前後の話者ラベルの連続性を、D の同じ時刻の出力と並べて見る。

### レポート末尾

採用した場合に「消える既存の作り込み」と「残る既存の作り込み」を列挙すること。

→ **停止。**

---

## 9. フェーズ 3 — 判断と設計への反映（着手前に必ず確認を取る）

コードは書かない。設計書のみ。

- **判定2 が ◯ のバックエンドがある場合**: そのバックエンドでは `redistribute_times()` が不要になる。「Gemini 固有の欠陥修正をバックエンド層に移す」という現行の抽象化方針が変わるため、見直し案を書く。
- **判定4 が ◯ の場合**: チャンク分割を前提としない処理経路が必要になる。既存のチャンク基盤を残したまま「分割しない」を選べる構造にする案を書く。
- **どれも × の場合**: A / local の現行構成を維持。`spike/stt-backends` は記録として残し、評価結果だけ `claude/` に置いて終了。

**結果にかかわらず共通で必要な提案:**

1. **キャッシュキーの件は PR #11 で解決済み。マージすれば足りる。**
   v3 はこれを「共通で必要な提案」として挙げていたが、**既に実装され、テストで
   機械的に縛られている。** `pipeline.py` に `CACHE_KEY_ENGINE_FIELDS`
   （`mode` / `model` / `model_dir`）と `CACHE_KEY_OTHER_FIELDS`（音声指紋 /
   チャンク長 / 言語 / 逐語フラグとそのプロンプト版 / 名簿 / `cluster_only`）が
   除外理由つきで宣言され、`tests/test_cache_key.py` が
   「`EngineSpec` に項目を足したら、鍵に入れるか除外するか分類するまで落ちる」
   形で縛っている。同じ事故を 4 回起こした記録もコメントに残っている。
   **したがって提案として書き直すことはしない。** 代わりに次の 2 点だけを確認する。
   - 新しいバックエンドを足すとき、`EngineSpec` に項目が増えるなら
     **KEYED / NOT_KEYED のどちらかに分類する**（検査が要求する）
   - **D のようにチャンク分割しない経路ができると `chunk_seconds` が鍵として
     意味を失う。** 現行実装は `if chunk_seconds:` なので 0 だと鍵から丸ごと落ちる。
     **ここは未解決なので、分割なし経路を提案する場合は必ず併せて提案する**

2. **取り込み経路（インポータ）の優先度を評価する。** §3 の通り、競合調査
   （`claude/競合比較と汎用化の方向性.md`。**この文書は近く `claude/` に追加される。
   追加後に読むこと**）の結論は「バックエンドを増やす」ではなく
   「入出力をフォーマット非依存にする」だった。RTTM / SRT / VTT / WhisperX JSON の
   取り込みを1本作れば全候補を一度にカバーできる。**バックエンド追加とインポータ追加の
   工数と効果を比較し、どちらを先にすべきかの見解を書くこと。**
   **この比較には実測の裏付けがある。** `tools/report_verbatim.py`（290 行の簡易
   インポータ 1 本）があったために、aTrain / Notta / AmiVoice / Gemini 各世代が
   追加の人手ゼロで同じ正解に載った。**工数比の実例としてこれを引くこと。**

   > **引用してよいのは §1（市場の前提）と §3（汎用化と需要）に限る。**
   > 同文書の冒頭にある「この文書の読み方（2026-08-30 追記）」が明示しているとおり、
   > **§2（本アプリの優位性）の実装に関する記述は 2026-07-31 時点のもので、現状と
   > 食い違う。** 声質特徴量は sherpa-onnx に置き換え済み、実会議での検証は実施済み、
   > ブランチの並走は決着済みである。**§2 の数字を引くと古い前提で論じることになる。**
   > 参照の中心は §3 の「**勝ち筋の汎用化 — 話者確定の工程だけを切り出す**」である。

3. **`reviewed`（✓/△）の位置づけを冒頭に明記する。** どのエンジンを使ったかに関係なく「人が耳で確認した」という記録が成果物の価値であり、エンジンの入れ替えはその主張を弱めない。むしろバックエンドが増えるほど価値は上がる。
   **判定1 の捏造の論点はここに直結する。** 6 エンジンの和集合でも 33 件中 14 件は
   どれも拾えず、捏造を検出できるのは人が聴く工程だけである。
   **§4-4 の `smart` の書き換えも同じ論点である。** もっともらしい別の語に
   静かに置き換わる誤りは、読んだだけでは気づけない。

4. **ローカル完結（faster-whisper + sherpa-onnx）はこの評価の影響を受けない。** 結論に「ローカル路線を見直す」を含めないこと。
   **ただし §2-3 のとおり、ローカル路線は「話者分離」と「ASR」の 2 つに分けて
   考えること。** このスパイクの結果が影響しうるのは後者だけであり、
   話者分離（sherpa-onnx）の成果はどの結論でも残る。

---

## 10. 成果物

- `tools/bench_gemini35.py`（B）
- `tools/bench_voxtral.py`（D）
- `tools/bench_chirp3.py`（**B と D が両方落ちた場合のみ**）
- `tools/report_timing.py`（判定2 と判定1 の内訳・唯一の新規設計）
- `claude/claude_stt_バックエンド評価結果.md`
- フェーズ 0.5 の比較表（チャットで報告し、必要なら `claude/` に）
- **既存の測定表への追記**（`claude/claude_ローカル化_技術方針書.md` §11.5 の
  6 系統表に B / D の行を足す案。**追記は人間の承認を得てから**）

---

## 11. 判断を仰いで停止すべき場面

- SDK のバージョンアップやインストールが必要と判明したとき
  （**C / D は着手時点で確実に該当する。** `google-cloud-speech` / `mistralai` が未インストール）
- GCP / Mistral の API 有効化・課金設定・アカウント作成が必要になったとき
  （**C は gcloud の再認証も該当する。** §4-2）
- 音声長の上限のため既存音源をそのまま扱えないと判明したとき
- **打ち切りが検出されたとき**（§7-3）。リトライせず、分割を細かくするかどうかは人間が決める
- **`src/` に手を入れないと評価できないと判明したとき**
- **`src/evaluate.py` の指標定義を変えないと測れないと判明したとき。**
  絶対に変えずに停止する。定義を後から変えれば都合の良い値を選べてしまう
- **起点の `main` に `tools/report_verbatim.py` が無いとき**（PR #11 が未マージ）
- 公式ドキュメントの記載が §4 の内容と食い違うと判明したとき
  （**§1-1 の表で扱いを決め、食い違いの内容を必ず報告する**）

いずれも勝手に進めず、状況と選択肢を提示して停止すること。
