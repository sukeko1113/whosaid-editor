# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
ENV/
*.egg-info/
.mypy_cache/
.pytest_cache/
.ruff_cache/

# PyInstaller / ビルド成果物
build/
dist/
Output/
*.spec.bak

# 同梱バイナリ・アセット(各環境/CIで配置)
ffmpeg/ffmpeg.exe
ffmpeg/ffmpeg
resources/icon.ico

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.swp
*.swo

# ローカル設定・秘密情報
.env
.env.*
config.local.json

# tests
__pycache__/
.pytest_cache/

# 動作確認用に repo 直下へ展開したポータブル版・ビルド成果物
# (上の build/ dist/ Output/ では拾えない位置に置かれることがある)
GeminiTranscriber.exe
GeminiTranscriberSetup-*.exe
_internal/
gemini-transcriber/

# ffplay も同梱バイナリ(ローカル/CI で配置)
ffmpeg/ffplay.exe
ffmpeg/ffplay

# 話者分離のモデル(ffmpeg と同じ扱い。各環境/CI で配置し、ビルドで同梱する)
#   models/diarize/sherpa-onnx-pyannote-segmentation-3-0/model.onnx  … MIT
#   models/diarize/nemo_en_titanet_small.onnx                        … CC-BY-4.0
models/

# 動作確認用の音声と作業ファイル
_tmp/
*.m4a
*.mp3
*.wav
*.WAV
*.speakers.json

# 製品が出力先に作る作業フォルダ(音声チャンク・転写キャッシュ・単語時刻・
# 話者分離の結果)。**出力先をリポジトリ内に指定した瞬間に追跡対象になる。**
# 上の _tmp/ や *.wav では拾えない(中身は JSON が主)。
# models/(510MB)を巻き込みかけたのと同じ形で、実害が出るまで気づかない。
.work_*/
