-r requirements.txt
pytest>=8.0

# 自動点検(Step 1-3)の実測用アライナ。torch は引かない(CTranslate2 + PyAV)。
# 製品の依存に上げる = インストーラに同梱するかは、点検を画面に繋ぐとき
# (設計書 §9/§10)に決める。それまでは開発側だけに置く。align.py は
# 未導入でも import でき、使おうとしたときに導入方法を知らせる。
faster-whisper>=1.2,<2
