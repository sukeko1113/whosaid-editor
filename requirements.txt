google-genai>=1.0.0,<3
python-docx>=1.1.0

# ローカル転写(本文の生成)と、自動点検の時刻の物差し。torch は引かない
# (CTranslate2 + PyAV)。ローカル既定化にあたって開発依存から製品依存へ上げた
# (claude/claude_ローカル転写_設計書.md §9)。
faster-whisper>=1.2,<2

# 話者分離(声のまとまりを作る)。これが無いと、ローカル経路は全区間が `?` に
# なって一括適用が働かない —— 割当の操作回数が 1219 回から 6 回に減るかどうかの
# 差なので、あれば良い部品ではなく製品依存である
# (claude/claude_話者分離_設計書.md §9)。
# こちらも torch を引かない(ONNX Runtime のみ)。
sherpa-onnx>=1.13,<2
