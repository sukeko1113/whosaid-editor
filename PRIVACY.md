# 何を送り、何を送らないか

Whosaid 反訳エディタ（以下「本アプリ」）が、あなたの端末の外へ何を出すかを
まとめたものです。**録音は機微情報である**という前提で作っています。

最終更新: 2026-08-23 / 対象バージョン: 2.2.0

---

## 要点

**既定の設定では、録音も本文も、あなたの端末から出ません。**

文字起こしも、声のまとまりを分ける処理も、すべて端末の中で動きます。
API キーも要りません。ネットワークを遮断したままでも使えます。

外へ出るのは、**あなたが「クラウドへ送って処理する」を明示的に選んだとき**だけです。

---

## 1. 通信が起きる場面

本アプリが外部と通信するのは、次の 3 つだけです。

### 1-1. クラウド（Gemini）を選んだとき

処理経路で「クラウド（Gemini）へ送って処理する」を選ぶと、**音声ファイルが
Google の Gemini API へ送られます。** 出席者の名簿を入力している場合は、
それも一緒に送られます。

- 送り先: Google LLC（Gemini API）
- 送るもの: 音声ファイル、出席者の名簿（入力している場合）、処理の指示文
- 受け取るもの: 文字起こしの結果

送った音声は、処理が終わった時点で削除を要求します。**途中でエラーが起きた
場合も削除します。** 削除が失敗した場合も、Google 側で 48 時間後に自動削除
されます。

Google がその音声をどう扱うかは、**あなたが使う API キーの種類によって変わり
ます。** 無償枠の API では、入力と出力が製品改善に利用され、人によるレビューの
対象になる場合があると記載されています。有償設定の Cloud Project 経由では、
製品改善には使われないとされています。

**機密情報・個人情報を含む録音を、無償枠の API で処理しないでください。**
条件は Google の規約が優先されるため、必ず最新のものを確認してください。

- https://ai.google.dev/gemini-api/terms

### 1-2. 文字起こしのモデルを取得するとき

より精度の高いモデル（large-v3・約 3GB）を使う場合、初回だけ配布元から
取得します。**取得するかどうかは、画面で一度だけ尋ねます。**

- 取得先: Hugging Face（Systran/faster-whisper-large-v3）
- 送るもの: **ありません。** 取得（ダウンロード）だけです

導入直後から使える小さいモデルは同梱しているので、この取得を断っても
本アプリは動きます。

### 1-3. GPU で動かす部品を取得するとき

GPU を積んだ端末で高速に処理する場合、必要な部品（cuBLAS）を取得します。
これも、画面で尋ねてから行います。

- 取得先: PyPI（nvidia-cublas-cu12）
- 送るもの: **ありません。** 取得だけです

---

## 2. 端末の中に保存されるもの

| 置き場 | 中身 |
| --- | --- |
| `%APPDATA%\WhosaidEditor\config.json` | 画面の設定、出席者の名簿、API キー |
| `%APPDATA%\WhosaidEditor\models\` | 取得した文字起こしモデル |
| `%APPDATA%\WhosaidEditor\cuda\` | 取得した GPU 用の部品 |
| 作業フォルダ（利用者が指定） | 割当作業ファイル、Word 出力、分割した音声、途中結果 |

**どれも外へ送られません。** 本アプリは利用状況の収集や、エラー報告の送信を
行いません。

### API キーの扱い

クラウド経路を使う場合に限り、API キーを設定ファイルに保存できます。
保存する場合は、Windows の仕組み（DPAPI）で暗号化し、**そのパソコンの、その
利用者アカウントでしか復号できない**形にします。

**暗号化できなかった場合は、保存しません。**何らかの理由で DPAPI が使えない
パソコンでは、キーをファイルに書かず、その旨をお知らせします。平文で保存する
ことはありません（その場合、起動のたびに入力していただく形になります）。

なお、旧版で平文のまま保存されたキーは、そのまま読み込めます（設定を引き継ぐ
ため）。**次に設定が保存された時点で暗号化された形に置き換わります。**すぐに
消したい場合は、設定画面の［消す］をお使いください。

「保存しない」を選べば、起動のたびに入力する形になり、ファイルには残りません。
保存済みのキーは、設定画面の［消す］で削除できます。

**キーの管理責任は利用者にあります。** 共有パソコンで使う場合は、保存しない
設定を勧めます。

---

## 3. 作った反訳の扱い

本アプリが作る作業ファイルには、**発言内容、時刻、話者、出席者の名簿、
編集の履歴**が入ります。元音声を特定するための指紋（ハッシュ値）も入りますが、
音声そのものは入りません。

これらのファイルの保管と受け渡しは、利用者の責任で行ってください。
**本アプリは、これらをどこへも送りません。**

---

## 4. 個人情報について

**声は、それ自体が個人情報になり得ます。** 本人確認のために生成された声の
特徴量は、個人識別符号にあたる場合があります。

会議の録音には、参加者の氏名・所属・発言内容が含まれます。録音、文字起こし、
第三者への提供に必要な権利や同意の確認は、**利用者の責任で**行ってください。

業務で使う場合は、所属組織の規程と、適用される法令（個人情報保護法など）に
従ってください。クラウド経路を使う場合は、外国にある第三者への提供にあたる
可能性があります。

---

## 5. 連絡先

不明な点や、この文書の誤りを見つけた場合は、GitHub の Issue でお知らせください。

https://github.com/sukeko1113/whosaid-editor/issues
