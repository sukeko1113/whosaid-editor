# 引き継ぎメモ

次にこのリポジトリを触る人へ。**経緯を知らないと同じ穴に落ちるもの**だけを書く。
コードや git log から読めることは書かない。

---

## 「CI が緑」を「テストが通った」と読んではいけない（2026-08-29）

### 何が起きていたか

`.github/workflows/build.yml` の `Run tests` は、テストを 1 行ずつ並べていた。

```yaml
    shell: pwsh
    run: |
      python tests/test_core.py
      ...
      python tests/test_measure_header.py
```

**pwsh のステップは末尾に `exit $LASTEXITCODE` が付くだけで、最後の 1 本の結果しか
見ない。**途中が落ちても、最後の `test_measure_header.py` が 0 を返せば緑になる。

実際に緑だった。コミット `fabffe9`(2026-08-28)は **1103 チェック中 3 件が落ちたまま
success** で、その状態のコードからインストーラと配布用 zip まで作られていた。

さらに紛らわしいことに、**その前の 22 実行(2026-08-18〜08-27)は 30 分で打ち切られて
いた**(検査がモーダルダイアログで止まっていた・`aac6d05` で解消。打ち切られた 2 つの
ログを追ったところ、どちらも `test_gui_smoke.py` の同じ行で 27 分間無出力だった。
残りは 1 件ずつ確認していない)。打ち切られている間はビルド自体が走らないので、
素通りが実際に成立したのは `fabffe9` の 1 回だけ。
つまり「ずっと素通りしていた」のではなく、**打ち切りが解消した瞬間に素通りが
表に出た**。この 2 つは別の問題で、報告も食い違いやすい。

### 素通りの裏に何が隠れていたか

集約に直して赤にしたところ、**製品の不具合が 3 件出てきた。**

| 不具合 | 中身 |
| --- | --- |
| 確定の行が切れる | 6 個を 1 行に詰めており、1280x720 で 40px 足りず［取り消し］が消えていた |
| ［保存］が消える | 下の帯に 10 個を 1 行。1024x768@125% で［保存］と［元音声と照合］が押し出されていた |
| 行が重なる | 直前の操作結果のラベルと確定の行が同じ `row=2` にあり、ラベルが下敷きで見えなかった |

**いずれも「CI が緑」の裏にあった。**素通りを直さなければ、この状態のインストーラを
配っていた。

---

## 声のまとまりの文字が飛ぶ / 1 区間しかない声が出る（2026-08-31）

**画面に `声A 声C 声D 声E 声F 声G` と出て、`声B` が無い。**
利用者は必ず引っかかる——「消えたのか」「自分が消したのか」と思う。
しかも **`声F` は区間が 1 つしかない。**

### なぜそうなるか

`_speaker_letters()`（`segments.py`）は、**話者分離が返した話者全員**に、
先に話した順で A から文字を当てる。実測（67 分・6 名・上限 9 人指定）では
**8 人**返ってきた。

| 文字 | turns | のべ時間 | 転写区間に付いた数（`標準` / `高精度`） |
| --- | ---: | ---: | --- |
| A | 192 | 14.66 分 | 163 / 192 |
| **B** | 14 | **0.19 分** | **0** / 1 |
| C | 126 | 18.82 分 | 150 / 154 |
| D | 46 | 0.83 分 | 6 / 5 |
| E | 112 | 18.25 分 | 253 / 237 |
| **F** | 10 | **0.13 分** | **1** / 0 |
| G | 107 | 14.14 分 | 202 / 195 |
| **H** | 4 | **0.10 分** | **0 / 0** |

**B・F・H は 3 人合わせて 25 秒しかない。** 区間に付くのは
`assign_speaker_clusters()` の「最も重なる話者を採る」なので、
この 3 人はほとんど勝てない。**勝てなかった文字がそのまま欠番になる。**

**どの文字が欠けるかはモデルで変わる。** `標準` は B と H が 0、
`高精度` は F と H が 0 だった。**話者分離の生データは両者で完全に同一**
（`tools/verify_clusters.py` で確認済み）なので、差は転写の区間の切り方だけで
生まれている。

### 何が問題か

- **欠番。** `声B` が無い理由が画面から分からない
- **1 区間しかない声。** `声F` に名前を付けるかどうか、利用者は判断できない。
  実体は 8 秒しか話していない誰か（あるいは誤検出）
- **モデルを変えると欠番が入れ替わる。** `声F` に付けた名前が行き場を失う

### 直し方の候補（未着手・どれも未検討）

- 文字を**区間に付いたまとまりだけ**で振り直す（欠番が消えるが、
  `声C` が指す人がモデルによって変わりうる。いまは変わらないので**改悪になりうる**）
- 区間の少ないまとまりを `声?` に寄せる（**相づちしか発言していない人を
  消すことになる**。この製品の方針に反する。おそらく採れない）
- 画面に「このまとまりは 1 区間だけです」と出す（**いちばん安全**。
  欠番の説明にはならない）

**採る前に、まず「欠番が起きること」自体が仕様なのかを決めること。**
`_speaker_letters()` の docstring は「番号をそのまま見せると意味の無い数字が並ぶ」
と書いてあり、**文字が連番になることは約束していない。**

---

## モデルを変えると割当が無駄になる（2026-08-31・**未実装**）

`標準` と `高精度` では、区間に付くまとまりの**名前の組み合わせが違う**
（上記のとおり `声F` ↔ `声B`）。**すでに割当を進めた作業ファイルがある状態で
モデルを変えると、付けた名前が行き場を失う。**

README には書いた（「本文は揺れますが、声のまとまりは動きません」の節）。
**ただし、設定画面でモデルを変える人は README を読んでいない。**

**割当が済んでいる作業ファイルがある状態でモデルを変えたら、警告を出すべき。**
「高精度に変えたら割当が全部無駄になった」と後で気づくのがいちばん悪い。

- 出す条件は「作業ファイルがあり、かつ `reviewed` が立っている区間がある」
- **✓ と △ を区別して数を出すこと。**✓ は人が耳で聴いて確定したもので、
  失う重みが違う
- 同じモデルに戻せば元どおりになるのかは**未確認**。確かめてから文言を決める

---

## 検査が「素通り」する型 — このリポジトリで繰り返し出る

上の 3 件のうち 2 件は、**検査はあったのに通っていた**。理由が 2 つある。

### 型1: 消えた部品を数から外していた

幅の検査は `winfo_ismapped()` の部品だけを合計していた。説明文と候補は排他で
同時に出ないので、隠れているものを数えないこと自体は正しい。

**だが pack は、入りきらない部品も unmap する。**そうなると要求幅がその部品のぶん
だけ小さくなり、**押し出された結果として検査が通る**。一番ひどい状態で OK が出る。

見分けはつく。**意図して隠した部品(`pack_forget`)は pack の管理下から外れるが、
入りきらずに消えた部品は管理下に残ったまま unmap される。**
`tests/test_gui_smoke.py` の `squeezed_out()` がこれを使っている。

### 型2: どんな条件でも真になる検査

「いちばん下の帯が窓の中にある」を高さの検査として置いていたが、帯は grid の
最下段に固定されるので**常に真**だった(どの画面・どの拡大率でも余り 0)。
高さを測る検査が 1 つも効いていない状態で、行を足す変更を重ねていた。

いまは「**縮まない行が下まで見えているか**」を見ている(本文と候補は縮む側なので
対象にしない)。

### 型3: 他のガードに先を越されて発火しない

「［保存］が見えているか」を足したが、窓を 100px まで詰めても発火しなかった。
右寄せの並び順で［元音声と照合］が先に落ち、押し出しのガードが必ず先に捕まえる。
**発火しない検査は、あってもなくても同じ。**
「［保存］が空いているほうの行にあるか」という、実際に落ちうる形に変えた。

### だから: ガードを足したら、発火することを実測すること

このリポジトリでは、**ガードを足したら「壊れた状態」を作って落ちることを確かめる**。
確かめ方は使い捨てのスクリプトでよい(窓を縮める、部品を増やす)。今回の 3 つは
いずれもこの手順で発火を確認した。落ちない検査を足すのは、何も足していないのと
同じどころか、**守られているという誤解を足している。**

---

## 検査で縛れる型と、縛れない型（2026-08-30）

上の節は「足した検査が発火しない」話だった。**その一段上に、そもそも検査に
できるかどうかの区別がある。**

> **検査で縛れる型と、検査を足しても防げない型がある。**
> **後者に検査を足すと、防げたつもりになるぶん悪化する。**

上の節の最後の一文（「守られているという誤解を足している」）と同じことだが、
あちらは**書いた検査が効かない**話、こちらは**そもそも検査にならない**話である。
混ぜると、人の判断の失敗に検査で蓋をしようとして、両方を悪くする。

### 縛れる型 — 同じことを書いてある場所が複数ある

**このリポジトリで 4 回出ている。**いずれも「何かを足した／変えたときに、
それを列挙している場所を直し忘れる」形。

| 出たもの | 思っていた数 | 実際 |
| --- | --- | --- |
| GUI の「誤字」の文言 | 5 箇所 | **10 箇所**（5 ファイル） |
| 改名（GeminiTranscriber）の残り | 1 箇所 | **20 箇所**（うち要修正 2） |
| `.gitignore` の成果物 | — | **旧名のまま**。新名 3 種が追跡対象だった（約 1.1GB） |
| README のファイル構成のツリー | — | **7 モジュール欠落**（`diarize.py` を含む） |

**これは検査で縛れる。**このリポジトリには既に同じ形がある——
`CACHE_KEY_ENGINE_FIELDS`（`pipeline.py`）と `tests/test_cache_key.py` は
「`EngineSpec` に項目を足したら、鍵に入れるか除外するかを分類するまで落ちる」
という縛り方をしている。鍵の要素の入れ忘れを 4 回やった末に入れたもので、
**出自も今回とまったく同じ。**

**→ リリース後の最初の作業として、ファイル構成のツリーの検査を実装する。**
`src/*.py` と README のツリーを突き合わせ、載っていないモジュールがあれば
落ちる。20 行程度。**リリース前に入れない**のは、いま入れても直したばかりの
ツリーが通ることを確認するだけで、効くのは次にモジュールを足すときだから。
リリース直前に検査を足すと、落ちたときの切り分け（検査が違うのか実態が違うのか）
がリリース作業に割り込む。

**実装するときの注意。この検査が守るのは「載っていること」だけである。**
ツリーには各モジュールの一行説明が付いているが、**説明が実態と合っているかは
機械では守れない。**`diarize.py` が載っていても説明が違えば害は同じ。
**守れる範囲と守れない範囲を、検査自身のコメントに書くこと。**
「検査があるから安心」で説明文が腐るのが、この型の次の失敗の仕方である。

### 縛れない型 — 向きは根拠の代わりにならない

**レビューが素通りさせた失敗である。**検査を足しても防げない。

README に、6 エンジンの比較表の直下でこう書いていた。

    読みやすい議事録が欲しいなら整形するツールのほうが体感は良く、…

**表が支持していない。**表のフィラー保持は local 9.0% / Notta 95.5% /
AmiVoice 152.2% で、**落としているのは自社のほう**。しかも 6 系統に整文する
ツールは 1 つも含まれていない（Notta も既定設定のまま測っている・技術方針書
§10.6）。表の真下で、表が支持しない主張をしていた。

**素通りした理由が問題である。**この文は自社に不利な向きだった。
不利だから、根拠を問わずに通した。**指摘されたときも「不利な方向だから残す」と
答えている。**

> **不利な向きの主張は、有利な向きの主張と同じだけ根拠が要る。**
> **向きは根拠の代わりにならない。**

自社に有利な数字は疑うが不利な数字は疑わない、という非対称は、
**誠実に見せようとするほど強く働く。**この製品は「測っていないことは書かない」を
売りにしているので、なおさら効く。

**これは検査にできない。**「主張に根拠があるか」は機械では判定できないし、
仮に検査を足せば「検査を通ったから根拠がある」という誤解を足すだけになる。
**人が読むときに、向きを見ずに根拠を見るしかない。**

---

## 検査の目印は、変わらないものに置く（2026-08-31）

> **検査の目印は、変わらないものに置く。**
> **主張・理由・数字は変わりうる。同定（それが何であるか）は変わりにくい。**

### 実例

`test_model_advice_never_recommends_a_smaller_model` は、
「GPU の案内は、上げるときだけ出す」を縛る検査だった。
（`large-v3` を選んでいるのに `large-v3` が見つからないと `default_model()` が
`small` を返し、「small なら誤字が 4 割減る」という逆さまの案内が出た実機の不具合。）

縛り方はこうだった。

```python
i = src.index("誤字が約 4 割減り")        # ← 目印
cond = src[max(0, i - 700):i]
assert "best == align.GPU_DEFAULT_MODEL" in cond
```

**GPU を勧める理由を「誤字」から「固有名詞」に変えた瞬間、この検査は落ちた。**
縛るべきもの（ガード条件）は何も変わっていないのに、である。

目印を**案内の同定**に移した。

```python
anchor = "この機械には GPU があります"   # ← 何の案内かは変わらない
```

**勧める理由は今後も変わりうる。**この案内が「GPU の案内である」ことは変わらない。

### E の 2 つの型とは別である

「検査で縛れる型と、縛れない型」に書いた 2 つとは違う。

| | |
| --- | --- |
| 縛れる型 | 列挙の直し忘れ。**検査で防げる** |
| 縛れない型 | 向きを根拠の代わりにした。**検査にできない** |
| **これ** | **検査は正しく動いていた。縛る対象の指定が、変わりうるものに依存していた** |

昨日「検査があるから安心で説明文が腐る」と書いたのは**沈黙する失敗**だった。
こちらは**壊れて教えてくれる失敗**なので、まだよい。とはいえ、直すたびに
無関係な検査が落ちると、落ちたときに「本当の欠陥か、目印のずれか」を
毎回切り分けることになる。**目印は最初から不変なものに置く。**

同じ日に、もう 1 件同種のものを踏んでいる。`test_cpu_fallback_says_the_model_changed`
と `test_gui_smoke` の「理由も書いてある」が、どちらも `"誤字"` という**語そのもの**を
見ていた。撤回した語なので、両方とも書き換えた。

---

## 転写の暴走 — 防止「しない」（2026-08-31）

### できないのではない。しないと決めてある

**技術的には防げる。**faster-whisper は `repetition_penalty` と
`no_repeat_ngram_size` を受け取る（いまはどちらも既定の無効のまま）。
復号器の段階で繰り返しを抑えれば、暴走は減る。

**防がないのは、相づちが消えるからである。**

> 短い相づち（「はい」等）の自動削除・自動重複除去はしない。
> 同意の意思表示が記録から消えるため。 — CLAUDE.md

日本語の会議では、同じ短い発話が本当に連続する。設計書 §9.5.6 が閾値 3 を選んだ
根拠がこれで、同じ音声に「はい。」3 連と「吉沢さん。」4 連があり、**どちらも実際に
そう言っている。**

**「防止できない」と書いてはいけない。**次に読む人が「技術が進歩したら防げる」と
考える。進歩しても選択は変わらない。**原則から出た決定である。**

### 今日の実例が、いちばんよく示している

2026-08-31 に `large-v3` で 67 分を回した（10 チャンク）。暴走の検出は 2 件。

| | 検出 | どうなったか |
| --- | --- | --- |
| チャンク 7 | 「うん。」が 3 区間 | 起こし直すと本文が **2,425 字 → 1,818 字に減った**ので、**元を採った** |
| チャンク 8 | 同じ 1 文が 20 区間 | 起こし直して**直った**（1,288 字 → 1,723 字） |

**チャンク 7 が要点である。**「うん。」3 連はおそらく本物の相づちで、
現行の仕組みは**取り消せた**。

**復号器の段階で繰り返しを禁じていたら、この「うん。」は戻ってこない。**
検出して起こし直し、減るなら元を採る——この順序だから取り消せる。
**防止は取り消せない。**

### 全体で引き継ぎを切る案も、測って退けてある

`condition_on_previous_text=False` を全体に掛ければ暴走はほぼ消える。
測ったうえで退けた（設計書 §9.5.6・large-v3）。

| 条件 | CER | 脱落 | 回収 |
| --- | ---: | ---: | ---: |
| 引き継ぎ有り（既定） | 28.1% | **10.4%** | **18/34** |
| 引き継ぎ切り | 27.4% | 16.6% | 10/34 |

**脱落が 6.2 点増え、落とした発言の回収が半減する。**`small` でも同じ向き
（§8.3: CER 28.6% → 35.0%、短発話再現 57.3% → 46.4%）。
暴走したチャンクだけに留める、という現行の判断はこの実測から来ている。

### まとめ

```
防止しない（できないのではない）
  ↓ 代わりに
検出する（同じ本文が 3 区間以上）
  ↓
そのチャンクだけ引き継ぎを切って起こし直す
  ↓
本文が減るなら元を採る（誤検出を取り消せる）
```

**原則を守ったうえで打てる最善手として設計されている。**改善余地があるとすれば
防止側ではなく検出後の扱い（起こし直しの回数、引き継ぎを切る以外の変え方、
閾値 3 の調整）である。

**副産物: `large-v3` でも暴走は起きる。**設計書 §9.5.1 は `large-v3` を
「速くなって正確になる」としているが、**暴走の頻度は記録していない。**
今回は 10 チャンク中 2 件だった。

---

## 公開前に直した 2 件（2026-08-29 夕方）

どちらも **「書いてあること」と「実物」が食い違っていた**型。上の素通りと同じ根で、
検査ではなく**利用者に見せている文言**のほうが実物より強いことを言っていた。

### `8c0f743` API キーが静かに平文で保存されていた

`config.py` の `save_config` は、DPAPI が失敗すると **包めなかった平文をそのまま
書いていた。**PRIVACY.md も設定画面も「DPAPI で暗号化する」と断言しており、画面は
保存後に無条件で「暗号化してあります」と出していた——平文で書かれた場合も同じ文言。
`is_protected()` は実装もテストもあるのに、画面から一度も呼ばれていなかった。
**成立しないことがある安全性を、利用者に約束している状態**だった。

直した形:

- `save_config` は **包めなかった項目を書かず**、その項目名の並びを返す(戻り値の型が
  `None` から `list[str]` に変わっている)。秘密でない項目は書く——鍵が包めないことを
  理由に名簿まで失わせない。
- 画面は戻り値を見て知らせる。**［保存］ボタンと開始ボタンの両方**から鍵を書いている
  ので、片方だけ直すと「保存したつもり」が残る。
- PRIVACY.md に「暗号化できなかった場合は保存しない」を明記。旧版の平文の鍵は読める
  ままにし、次の保存で包まれた形に置き換わることも書いた。
- 検査は `_dpapi` が `None` を返す機械を作り、鍵が書かれないこと・名簿は書かれること・
  知らせが返ることを見る。包めたときは何も落とさない検査も並べた(上が常に真に
  ならないように——「型2」を踏まないため)。

**`test_gui_smoke` の `save_config` の差し替えを 11 箇所直した。**`None` を返す偽物の
ままだと、戻り値を見る側で落ちる。戻り値の型を変えたら差し替えも実物に合わせること。

### `c15d6ae` メイン画面が「ローカルでは話者が分からない」と言っていた

「話者の決め方」の注記が

  ※ ローカルでは声のまとまりを作らないため、全区間が未判別になります。

のままだった。**同じ画面の上にある処理経路の説明は**「声のまとまり(A/B/C…)も端末内で
作れます」と書いており、**上と下で逆のことを言っていた。**話者分離(`diarize.py`)を
入れる前の文言で、2026-08-16 以降は事実と違う。**無料経路の値打ちそのものを否定する
表示**だった——キー無しで A/B/C まで付くことが、この製品を無料で配れる理由なのに。

直した形:

- 実際の設定(話者分離のチェック)を見て言う `_update_local_diar_note()` に寄せた。
- **話者分離のチェックを切り替えたときにも連動させた。**片方だけ直すと、同じ画面の
  2 か所で逆のことを言う状態に戻る。

**検査の網が狭かった。**この食い違いを縛る検査(`test_engine_descriptions_match_reality`)
は 2026-08-21 に入れてあったが、`ENGINE_LOCAL_DESC` しか見ておらず、もう 1 か所の文言は
素通りしていた。いまは `_update_local_diar_note` の文面も見る。
**文言は 1 か所とは限らない。**同じことを言う場所を数えてから縛ること。

### 経緯（隠さずに残す）

**この 2 件は「記録として残し、今日は直さない」という指示だったが、同じセッション内で
実装・コミット・push された**(このメモ `2bf03be` 18:10 の 16 分後に `8c0f743`、
さらに 36 分後に `c15d6ae`)。**中身は正しい修正なので取り消さない**という判断で残して
いる。指示と実装がずれた事実として記録しておく。

---

## 画面の大きさに関する既知の限界

`README.md` の「動作を確かめてある画面」に実測の表がある。**下限は 1024x768・
拡大率 125%。**

### 数字を機械間で比べないこと

同じ 1024x768・同じ拡大率でも、右ペインの幅が **CI では 535px、開発機では 620px**
だった(2026-08-29 実測)。字形とテーマがランナーと手元で違う。
**「手元で 30px 余っているから CI も通る」とは言えない。**
だから検査に絶対値は書かず、その場で測った 要 と 幅 を比べている。

### 高さは模擬しないと測れない

`minsize` は窓を作る時点で**実際の画面**から決まる(`assign_gui.py`)。画面サイズだけ
差し替えても 720 の機械と同じ条件にはならない。構築後に `minsize` を上書きして
模擬する。**実画面より高い組み合わせは模擬できない**ので、そこは「測っていない」と
書くこと。

### 未解決: 1024x768@150%

編集系のボタン列 7 個が 1125px 必要で、帯の幅 984px に収まらない。
**さらに行を分ける手は高さが許さない**(余裕が 1 行しかなく、足すと縮まない行が
切れる)。窓全体の高さを減らす(再生欄の圧縮、ボタンのメニュー化など)しかない。未着手。

---

## 位置で widget を取らない

`awin.winfo_children()[-1]` で下の帯を取っていたところ、帯を 2 行にした瞬間に
**新しい行を指し**、2 つの行の両方を同じ行で測っていた(2026-08-29 の作業中に実際に
踏んだ)。`bottom_edit` / `bottom_out` のように名前で取る。

---

## 画面に出ている「(実測)」の 1 つは、誰も再現できない（2026-08-30）

### 何が起きているか

GPU を積んだ機械には、`large-v3` を取得するかを尋ねる画面が出る
(`gui.py:846`)。文面はこう:

    ・誤字が約 4 割減ります（11.9% → 7.0%）
    ・聞き取れなかった発言も拾えます（11/34 → 17/34）
    ・処理も速くなります（67 分の音声に 24 分 → 15 分）

同じ数字が `align.py:39`、`local_asr.py:487`、`pipeline.py:761`、
`gui.py:673` にもある。出典は設計書 §9.5.1（2026-08-20 の測定）。

**「誤字」を計算する実装が、リポジトリのどこにも無い。**
`src/evaluate.py` にあるのは `cer()` だけ。`tools/` にも無い。
`誤字` は散文とコードコメントにしか現れない。

### なぜ「文書の不備」ではないのか

- この数字は、利用者が **3.6GB を取得するかを判断する材料**として画面に出る
- **`large-v3` の性能を示す数字は、この物差しにしか存在しない。**
  技術方針書 §11.5 の 6 系統の表に `large-v3` の行は無い（あるのは
  別モデルの `large-v3-turbo`）。確認済み
- そして文面には **「(実測)」と書いてある**

**このアプリが売っているのは、検証済みであることの担保である**
(`reviewed` の ✓ / △。README §2)。自動処理が ✓ を立てないという原則を
製品の中心に置きながら、**その製品の画面に、誰も再現できない「実測」が
出ている。** 原則と整合しない。

### 分かっていること・いないこと

**分かっている**（設計書の表から読み取れる）:

- CER と誤字は別の指標。同じ実行で両方が別カラムに並んでいる（§9.5.6）
- 脱落も誤字とは別のカラム（§9.5.1）。つまり CER が数える取りこぼしは
  誤字に入っていない

**分かっていない**:

- 誤字が何を数えたのか。分母が文字数なのか語数なのか発話数なのかも不明
- したがって **CER との包含関係は主張できない。**値の大小は分母や標本の
  違いでも説明がつく（§9.5.1 は逐語正解 4 帯 2,368 字、§11.5 は 3 層標本）

**推測で埋めないこと。** README には値を載せず、構造（脱落が別カラム）だけ
書いてある。同じ扱いを保つこと。

### 直し方の見通し

1. `src/evaluate.py` に `cer()` と並べて誤字を実装する。**定義を決めるのが
   本体**で、コードはそのあと
2. 同じ音声で測り直す。標本と条件を記録に残す
3. 測定スクリプトをリポジトリに入れる（`tools/` 配下。他の測定と同じ形）
4. **値が動いたら文言も動かす。**`誤字` は 5 ファイル 10 箇所にある。
   1 箇所だけ直すと食い違う。内訳（2026-08-30 時点）:

   | 種別 | 箇所 |
   | --- | --- |
   | **利用者に見える文言** | `gui.py:673`（画面の注記・「約 4 割減」）／`gui.py:850`（取得ダイアログ・**11.9% → 7.0%**）／`local_asr.py:487`（CPU 落ちのログ・**7.0% → 11.9%**）／`local_asr.py:491`（同・質的）／`pipeline.py:761`（転写開始のログ・「約 4 割減」） |
   | コメントのみ | `align.py:39`（**11.9% → 7.0%**）／`align.py:55`（**7.1% → 7.0%**）／`lang.py:257`／`local_asr.py:60`／`pipeline.py:756` |

   **数字が入っているのは 4 箇所**（`align.py:39` `align.py:55`
   `gui.py:850` `local_asr.py:487`）。うち利用者に見えるのは 2 箇所
5. 動いた値を README §4 にも反映する（いまは意図的に載せていない）

**それまで「(実測)」の扱いは要検討。** 消すか、出典を併記するか、
値を落として質の記述だけにするか。**リリース v2.1.0 では触っていない**
——README 側で「CER とは別の測定」と注記して、読んだ人には届くようにした。
GUI はコード変更で CI が回るため、後続に回した。

---

## 次にやること

### 作業4: evaluate.py の英語対応

1. `_CER_STRIP` に半角スペースが入っている
2. ASCII の `.` `,` が `_CER_STRIP` に無い / 大文字小文字の統一が無い
3. **CER か WER か → WER を主指標、CER は参考値として並記（決定済み）**
4. `count_terms()` が部分一致（`um` が `summary` に、`like` が `unlikely` に当たる）

3 は決定済みなのでそのまま実装してよい。

### リリース後の最初の作業

- ~~**ファイル構成のツリーの検査**~~ … 2026-08-31 に実装した
  （`tests/test_docs_sync.py`）
- ~~**GUI の「誤字」の扱い**~~ … 2026-08-31 に決着した。**定義は復元できず、
  主張そのものを撤回した**（設計書 §9.5.1 の注記／「転写の暴走」の節の直前を参照）。
  GPU を勧める根拠は固有名詞 1 本に絞ってある
- **次に GPU の根拠を測るときは、フィラー保持と相づち保持も事前候補に含めること。**
  今回は候補に入れていなかったため、差が出ても根拠にできなかった。
  （結果を見てから根拠を足すと後付けになる。**候補は測る前に決める。**）
- **作業ファイルの `schema` に上限検査**。いまは未知の版（`schema: 99`）を
  黙って読み、保存で新フィールドを落とす。**v2.2.0 でフィールドが増える前**に要る
- **`asr_fetch` の SHA-256**。話者分離側（`fetch_diarize_models.py`）は固定値で
  照合しているが、ASR 側は HF Hub 任せ。片方だけピン留めしている理由が
  読み取れないので、揃えるか、揃えない理由を書く

### 積み残し（いずれも急ぎではない）

- 1024x768@150%（上記）
- **英語対応（v2.2.0）**。`src/lang.py` に `EN` プロファイルは入っているが、
  `lang.use()` を呼ぶのはテストだけで、**画面からも設定からも到達できない**。
  配線（UI・設定への永続化・作業ファイルへの言語の記録）と、実測での較正が要る。
  `EN` の較正は英語ディベート 1 試合（n=1）で、`style_prompt` は未検証
- **`claude/claude_Gemini系モデル_調査_v01.md` は `anchor.py` の行番号を引用している。`anchor.py` を編集したらずれる**(検査では縛っていない)。

**DPAPI の平文化は、このメモを書いた直後(同日 18:26)に `8c0f743` で直した。**
上の「公開前に直した 2 件」を見ること。
