# 引き継ぎメモ

次にこのリポジトリを触る人へ。**経緯を知らないと同じ穴に落ちるもの**だけを書く。
コードや git log から読めることは書かない。

---

## 「CI が緑」を「テストが通った」と読んではいけない（2026-08-29）

### 何が起きていたか

`.github/workflows/build.yml` の `Run tests` は、テストを 1 行ずつ並べていた。

```yaml
    shell: pwsh
    run: |
      python tests/test_core.py
      ...
      python tests/test_measure_header.py
```

**pwsh のステップは末尾に `exit $LASTEXITCODE` が付くだけで、最後の 1 本の結果しか
見ない。**途中が落ちても、最後の `test_measure_header.py` が 0 を返せば緑になる。

実際に緑だった。コミット `fabffe9`(2026-08-28)は **1103 チェック中 3 件が落ちたまま
success** で、その状態のコードからインストーラと配布用 zip まで作られていた。

さらに紛らわしいことに、**その前の 22 実行(2026-08-18〜08-27)は 30 分で打ち切られて
いた**(検査がモーダルダイアログで止まっていた・`aac6d05` で解消。打ち切られた 2 つの
ログを追ったところ、どちらも `test_gui_smoke.py` の同じ行で 27 分間無出力だった。
残りは 1 件ずつ確認していない)。打ち切られている間はビルド自体が走らないので、
素通りが実際に成立したのは `fabffe9` の 1 回だけ。
つまり「ずっと素通りしていた」のではなく、**打ち切りが解消した瞬間に素通りが
表に出た**。この 2 つは別の問題で、報告も食い違いやすい。

### 素通りの裏に何が隠れていたか

集約に直して赤にしたところ、**製品の不具合が 3 件出てきた。**

| 不具合 | 中身 |
| --- | --- |
| 確定の行が切れる | 6 個を 1 行に詰めており、1280x720 で 40px 足りず［取り消し］が消えていた |
| ［保存］が消える | 下の帯に 10 個を 1 行。1024x768@125% で［保存］と［元音声と照合］が押し出されていた |
| 行が重なる | 直前の操作結果のラベルと確定の行が同じ `row=2` にあり、ラベルが下敷きで見えなかった |

**いずれも「CI が緑」の裏にあった。**素通りを直さなければ、この状態のインストーラを
配っていた。

---

## 検査が「素通り」する型 — このリポジトリで繰り返し出る

上の 3 件のうち 2 件は、**検査はあったのに通っていた**。理由が 2 つある。

### 型1: 消えた部品を数から外していた

幅の検査は `winfo_ismapped()` の部品だけを合計していた。説明文と候補は排他で
同時に出ないので、隠れているものを数えないこと自体は正しい。

**だが pack は、入りきらない部品も unmap する。**そうなると要求幅がその部品のぶん
だけ小さくなり、**押し出された結果として検査が通る**。一番ひどい状態で OK が出る。

見分けはつく。**意図して隠した部品(`pack_forget`)は pack の管理下から外れるが、
入りきらずに消えた部品は管理下に残ったまま unmap される。**
`tests/test_gui_smoke.py` の `squeezed_out()` がこれを使っている。

### 型2: どんな条件でも真になる検査

「いちばん下の帯が窓の中にある」を高さの検査として置いていたが、帯は grid の
最下段に固定されるので**常に真**だった(どの画面・どの拡大率でも余り 0)。
高さを測る検査が 1 つも効いていない状態で、行を足す変更を重ねていた。

いまは「**縮まない行が下まで見えているか**」を見ている(本文と候補は縮む側なので
対象にしない)。

### 型3: 他のガードに先を越されて発火しない

「［保存］が見えているか」を足したが、窓を 100px まで詰めても発火しなかった。
右寄せの並び順で［元音声と照合］が先に落ち、押し出しのガードが必ず先に捕まえる。
**発火しない検査は、あってもなくても同じ。**
「［保存］が空いているほうの行にあるか」という、実際に落ちうる形に変えた。

### だから: ガードを足したら、発火することを実測すること

このリポジトリでは、**ガードを足したら「壊れた状態」を作って落ちることを確かめる**。
確かめ方は使い捨てのスクリプトでよい(窓を縮める、部品を増やす)。今回の 3 つは
いずれもこの手順で発火を確認した。落ちない検査を足すのは、何も足していないのと
同じどころか、**守られているという誤解を足している。**

---

## 公開前に直した 2 件（2026-08-29 夕方）

どちらも **「書いてあること」と「実物」が食い違っていた**型。上の素通りと同じ根で、
検査ではなく**利用者に見せている文言**のほうが実物より強いことを言っていた。

### `8c0f743` API キーが静かに平文で保存されていた

`config.py` の `save_config` は、DPAPI が失敗すると **包めなかった平文をそのまま
書いていた。**PRIVACY.md も設定画面も「DPAPI で暗号化する」と断言しており、画面は
保存後に無条件で「暗号化してあります」と出していた——平文で書かれた場合も同じ文言。
`is_protected()` は実装もテストもあるのに、画面から一度も呼ばれていなかった。
**成立しないことがある安全性を、利用者に約束している状態**だった。

直した形:

- `save_config` は **包めなかった項目を書かず**、その項目名の並びを返す(戻り値の型が
  `None` から `list[str]` に変わっている)。秘密でない項目は書く——鍵が包めないことを
  理由に名簿まで失わせない。
- 画面は戻り値を見て知らせる。**［保存］ボタンと開始ボタンの両方**から鍵を書いている
  ので、片方だけ直すと「保存したつもり」が残る。
- PRIVACY.md に「暗号化できなかった場合は保存しない」を明記。旧版の平文の鍵は読める
  ままにし、次の保存で包まれた形に置き換わることも書いた。
- 検査は `_dpapi` が `None` を返す機械を作り、鍵が書かれないこと・名簿は書かれること・
  知らせが返ることを見る。包めたときは何も落とさない検査も並べた(上が常に真に
  ならないように——「型2」を踏まないため)。

**`test_gui_smoke` の `save_config` の差し替えを 11 箇所直した。**`None` を返す偽物の
ままだと、戻り値を見る側で落ちる。戻り値の型を変えたら差し替えも実物に合わせること。

### `c15d6ae` メイン画面が「ローカルでは話者が分からない」と言っていた

「話者の決め方」の注記が

  ※ ローカルでは声のまとまりを作らないため、全区間が未判別になります。

のままだった。**同じ画面の上にある処理経路の説明は**「声のまとまり(A/B/C…)も端末内で
作れます」と書いており、**上と下で逆のことを言っていた。**話者分離(`diarize.py`)を
入れる前の文言で、2026-08-16 以降は事実と違う。**無料経路の値打ちそのものを否定する
表示**だった——キー無しで A/B/C まで付くことが、この製品を無料で配れる理由なのに。

直した形:

- 実際の設定(話者分離のチェック)を見て言う `_update_local_diar_note()` に寄せた。
- **話者分離のチェックを切り替えたときにも連動させた。**片方だけ直すと、同じ画面の
  2 か所で逆のことを言う状態に戻る。

**検査の網が狭かった。**この食い違いを縛る検査(`test_engine_descriptions_match_reality`)
は 2026-08-21 に入れてあったが、`ENGINE_LOCAL_DESC` しか見ておらず、もう 1 か所の文言は
素通りしていた。いまは `_update_local_diar_note` の文面も見る。
**文言は 1 か所とは限らない。**同じことを言う場所を数えてから縛ること。

### 経緯（隠さずに残す）

**この 2 件は「記録として残し、今日は直さない」という指示だったが、同じセッション内で
実装・コミット・push された**(このメモ `2bf03be` 18:10 の 16 分後に `8c0f743`、
さらに 36 分後に `c15d6ae`)。**中身は正しい修正なので取り消さない**という判断で残して
いる。指示と実装がずれた事実として記録しておく。

---

## 画面の大きさに関する既知の限界

`README.md` の「動作を確かめてある画面」に実測の表がある。**下限は 1024x768・
拡大率 125%。**

### 数字を機械間で比べないこと

同じ 1024x768・同じ拡大率でも、右ペインの幅が **CI では 535px、開発機では 620px**
だった(2026-08-29 実測)。字形とテーマがランナーと手元で違う。
**「手元で 30px 余っているから CI も通る」とは言えない。**
だから検査に絶対値は書かず、その場で測った 要 と 幅 を比べている。

### 高さは模擬しないと測れない

`minsize` は窓を作る時点で**実際の画面**から決まる(`assign_gui.py`)。画面サイズだけ
差し替えても 720 の機械と同じ条件にはならない。構築後に `minsize` を上書きして
模擬する。**実画面より高い組み合わせは模擬できない**ので、そこは「測っていない」と
書くこと。

### 未解決: 1024x768@150%

編集系のボタン列 7 個が 1125px 必要で、帯の幅 984px に収まらない。
**さらに行を分ける手は高さが許さない**(余裕が 1 行しかなく、足すと縮まない行が
切れる)。窓全体の高さを減らす(再生欄の圧縮、ボタンのメニュー化など)しかない。未着手。

---

## 位置で widget を取らない

`awin.winfo_children()[-1]` で下の帯を取っていたところ、帯を 2 行にした瞬間に
**新しい行を指し**、2 つの行の両方を同じ行で測っていた(2026-08-29 の作業中に実際に
踏んだ)。`bottom_edit` / `bottom_out` のように名前で取る。

---

## 次にやること

### 作業4: evaluate.py の英語対応

1. `_CER_STRIP` に半角スペースが入っている
2. ASCII の `.` `,` が `_CER_STRIP` に無い / 大文字小文字の統一が無い
3. **CER か WER か → WER を主指標、CER は参考値として並記（決定済み）**
4. `count_terms()` が部分一致（`um` が `summary` に、`like` が `unlikely` に当たる）

3 は決定済みなのでそのまま実装してよい。

### 積み残し（いずれも急ぎではない）

- 1024x768@150%（上記）
- PR #11 のマージ時期（10 本の測定後に判断）
- `main` が 152 コミット遅れている(2026-08-29 時点。`git rev-list --count origin/main..HEAD`)

**DPAPI の平文化は、このメモを書いた直後(同日 18:26)に `8c0f743` で直した。**
上の「公開前に直した 2 件」を見ること。
