# Gemini 文字起こし

長尺の音声ファイル(m4a / mp3 / wav 等)を文字起こしし、
**話者を確定させたうえで** 1 つの Word ファイル(.docx)にまとめる Windows デスクトップアプリです。

文字起こしは **2 つの経路**から選べます(既定はローカル)。

| 経路 | 中身 | 向き・不向き |
| --- | --- | --- |
| **ローカル(既定)** | この PC の faster-whisper で処理。録音も本文も外へ出ません。API キー不要 | 録音を外部へ出せない案件向け。**声のまとまり(A/B/C…)は作られない**ので、話者は 1 件ずつ確定します |
| クラウド | 音声を Gemini API へ送って処理 | 声のまとまりが付くぶん割当が速い。録音が外部サービスへ出ます |

---

## v2.0.0 の変更点: 話者の決め方が変わりました

**従来(v1.3)**: 出席者名簿を先に登録 → AI が名簿を見て「この発言は佐藤さん」と推定。
→ プロンプトが長くなって処理が遅く、しかも推定はよく外れました。外れた箇所を
Word 上で直すのは、音声と突き合わせる作業になり非常に手間でした。

**v2.0 から**: AI には **声質で A / B / C … に分けさせるだけ**。実名は、
専用の割当画面でタイムラインを区間単位でたどりながら、**その区間の音声を聴いて
候補リストから選ぶ**方式にしました。

割当作業は、進めるほど速くなります。

- **確定するたびに学習** します。同じ声のまとまり(クラスタ)、会話の流れ(誰の次に誰が話すか)、
  発言頻度から、**可能性の高い話者を候補リストの先頭に持ってきます**。
- 数字キーひとつで確定 → 自動で次の未確定区間へ移動 → 自動で再生。
  マウスに触れずに流し続けられます。
- **「同じ声のまとまり全体に適用」** を使うと、1 回の操作でそのクラスタの
  未確定区間すべてを確定できます。多くの場合、これが一番速い進め方です。
- 作業状態は自動保存されます。閉じても続きから再開できます。

---

## クイックスタート

1. [Releases](../../releases) から最新の `GeminiTranscriberSetup-x.y.z.exe` をダウンロード
2. インストーラを実行
3. スタートメニューから「Gemini 文字起こし」を起動
4. 音声ファイル選択 → 出席者を入力 → 「文字起こし → 割当画面へ」
   (既定のローカル処理なら API キーは要りません。クラウドを選ぶ場合だけ貼付)

---

## 使い方

### 1. 文字起こし(メイン画面)

1. **音声ファイル**を「参照...」で選択。
2. **出力フォルダ**は既定で *音声と同じフォルダ*。
3. **処理経路**を選ぶ。既定は「この PC で処理する(ローカル)」。
   クラウドを選んだときだけ **API キー**を貼り付けて「保存」(初回のみ)。
4. **話者の決め方**で「区間ごとに聴いて割り当てる(推奨)」を選ぶ。
   (「AI にすべて任せる(従来方式)」はクラウド専用です)
5. **出席者**を 1 行 1 人で入力(例: `佐藤(理事長)`)。
   よく発言する人を上に置くと、最初の候補順が良くなります。
   ここで入れなくても、割当画面から追加できます。
6. 「**文字起こし → 割当画面へ**」を押す。

完了すると `<音声名>.speakers.json`(作業ファイル)ができ、割当画面が自動で開きます。

### 2. 話者の割当(割当画面)

| | |
| --- | --- |
| 上部の帯 | 音声全体のタイムライン。色は話者、灰色は未確定。クリックでジャンプ |
| 左 | 区間の一覧。時刻 / 声のまとまり / 話者 / 発言。絞り込みは上部のラジオボタン |
| 右上 | 選択中の区間の全文(その場で修正できます) |
| 右中 | 再生。区間の前後に少し余白を付けて鳴らします |
| 右下 | 話者候補。**可能性の高い順**に並び、右側に理由が出ます |

一覧の話者欄に付く印:

| 印 | 意味 |
| --- | --- |
| (なし・黄色の行) | 未確定 |
| `△` | まとめて適用しただけ(**自分の耳では聴いていない**) |
| `✓` | その区間を聴いて確定した |

**キーボード操作**(これだけで一周できます)

| キー | 動作 |
| --- | --- |
| `Space` | 再生 / 停止 |
| `1`〜`9` | その番号の候補で確定 → 次へ |
| `Enter` | 一番上の候補で確定 |
| `↑` `↓` / `k` `j` | 区間を移動 |
| `Tab` / `Shift+Tab` | 次 / 前の「まだ手を付けていない区間」へ |
| `A` | 「同じ声のまとまり全体に適用」の ON/OFF(既定 ON) |
| `U` | 発言者不明にする |
| `D` | 未確定に戻す |
| `Ctrl+Z` | 取り消し |
| `Ctrl+S` | 保存 |

**おすすめの進め方(2 パス)**

1. **1 パス目 — まとまり単位でざっと埋める。**
   `A`(同じ声のまとまり全体に適用)は既定で ON です。各まとまりの代表を 1 つ聴いて確定すると、
   そのまとまりの未確定区間がすべて同じ話者になります。
   15 分チャンク × 6 個・6 名の会議なら、おおむね **30〜40 回**の操作で全区間が埋まります。
   まとめて適用したぶんは `△`(未確認)として記録されます。
2. **2 パス目 — `△` を聴いて確かめる。**
   一覧上部を「**未確認のみ**」に切り替えると `△` の区間だけが並びます。`Tab` でたどり、
   聴いて正しければ同じ候補をもう一度選べば `✓` になります。違っていればその場で直します。
   ここを飛ばすと「速いが検証していない議事録」になるので、重要な会議では必ず通してください。
3. 間違えて一括適用したときは `Ctrl+Z`、あるいは下部の「このまとまりを未確定に戻す」。
4. 「**Word で出力...**」。

> **声のまとまりはチャンクごとに独立しています。**
> 「C1-A」と「C2-A」は別のまとまりです(AI はチャンクごとに独立して声を聞き分けるため)。
> ただし候補の学習は会議全体で共有され、
> **チャンク境界の連続性**(切れ目は発言の途中に落ちるので、前チャンク末尾と次チャンク先頭は
> 同じ人であることが多い)と**同じ記号の引き継ぎ**(前のチャンクの A が誰だったか)を
> 手がかりにするため、2 つ目以降のチャンクは最初の 1 手から候補が当たりやすくなります。
> チャンク長を長くする(既定 15 分 → 20 分など)と、まとまりの数自体が減って手数も減ります。

> **【?】【*】のまとまりには一括適用できません。**
> それぞれ「判別できなかった」「複数人が重なった」区間の寄せ集めで、同じ声ではないためです。
> 1 区間ずつ聴いて判断してください。

### 3. 出力

「**Word で出力...**」を押すと、出力の設定を選べます。

- **表題**(既定は音声ファイル名)
- 段落の先頭に時刻を入れるか(配布用の議事録なら外す / 反訳なら入れる)
- 同じ話者の連続発言を 1 段落にまとめるか
- 冒頭に出席者一覧を入れるか
- 「発言なし・雑音」と印を付けた区間を省くか
- 同じ内容の `.txt` も出すか(自分のテンプレートに貼り込む場合に便利)

冒頭には「全 N 区間中、聴いて確定 X 区間 / まとめて適用 Y 区間 / 未確定 Z 区間」という
但し書きが入ります。どこまで人手で確認したものかが、あとから読む人にも分かるようにするためです。
未確定の区間は【発言者不明】(赤字)、ユーザーが意図的に不明と判断した区間はグレーで出力されます。

### 中断・再開

- 割当作業は 4 秒ごとに自動保存されます。ウィンドウを閉じても失われません。
- 再開はメイン画面の「**保存済みの割当作業を開く...**」から `<音声名>.speakers.json` を選択。
- 文字起こし自体もチャンク単位でキャッシュされます(`.work_<音声名>\transcripts\`)。
  同じ音声で再実行すると未処理分だけが走り、**既存の割当結果も引き継がれます**。

---

## 詳細設定

- **処理経路**: `ローカル`(既定 / この PC で完結) / `クラウド`(Gemini へ送る)。
  ローカルでは API キー・逐語モード・従来方式は使えません(いずれも仕組みが無いため、
  画面上でも選べないようにしてあります)。
- **モデル**: 経路によって候補が変わります。
  - ローカル: `small`(既定) / `base`(速いが精度は落ちる) / `medium`(CPU では重い)
  - クラウド: `gemini-2.5-flash`(既定 / 速くて安価) / `gemini-2.5-pro`(高精度・高コスト)
- **チャンク長**: 既定 15 分。長いほど声の聞き分けが安定し、まとまりの数も減って
  割当が楽になりますが、アップロード/転写失敗のリスクは上がります。
  変更すると転写はやり直しになります(長さの違う転写を混ぜるとテキストと音声がずれるため)。
- **逐語モード**: フィラー・言い直しを残し、整文しません(反訳・記録用)。
- **AI にすべて任せる(従来方式)**: v1.3 の動作。名簿を AI に渡して実名まで推定させます。
  精度は落ちますが、割当作業をしたくない場合の選択肢として残しています。

---

## ファイル構成

```
whosaid-editor/
├─ src/
│  ├─ main.py        ... エントリーポイント
│  ├─ gui.py         ... メイン画面(Tkinter)
│  ├─ assign_gui.py  ... 話者割当エディタ            ★v2.0
│  ├─ pipeline.py    ... 分割→転写→セグメント化の制御
│  ├─ audio.py       ... ffmpeg 呼び出し(分割・長さ取得)
│  ├─ player.py      ... 区間再生(ffplay / winsound)  ★v2.0
│  ├─ segments.py    ... 区間・話者のデータモデルと Word 出力 ★v2.0
│  ├─ suggest.py     ... 話者候補の学習・並べ替え      ★v2.0
│  ├─ transcribe.py  ... Gemini API + 出力のセグメント化
│  ├─ local_asr.py   ... ローカル転写(faster-whisper)  ★
│  ├─ align.py       ... 時刻の実測(faster-whisper)
│  ├─ anchor.py      ... 転写と実測の照合
│  ├─ inspection.py  ... 自動点検の提案づくり
│  └─ config.py      ... 設定の永続化
├─ tests/            ... ユニット/結合/GUI スモークテスト ★v2.0
├─ ffmpeg/           ... ffmpeg.exe / ffplay.exe を置く(同梱されません)
├─ resources/        ... icon.ico を置く(任意)
├─ requirements.txt
├─ build.spec        ... PyInstaller スペック
├─ installer.iss     ... Inno Setup スクリプト
├─ build.bat         ... ワンクリックビルド
└─ README.md
```

### 作業ファイル(`<音声名>.speakers.json`)の中身

```jsonc
{
  "schema": 5,
  "audio_path": "C:\\...\\meeting.m4a",
  "duration": 3600.0,
  "source_sha256": "",     // 元音声の SHA-256。第三者が検算するための値
  // 処理経路の記録。Word の検証要約に「クラウド/ローカル」として出る
  "engine": { "mode": "local", "model": "small", "compute_type": "int8",
              "app_version": "2.1.0", "at": "2026-08-14T…Z" },
  "doc_revision": 0,       // Word を出力するたびに +1(書面に「版」として併記)
  "edit_log": [],          // 追記型の編集履歴(編集履歴の実装で記録される)
  "speakers": [ { "id": "sp01", "name": "佐藤", "note": "理事長", "order": 0 } ],
  "segments": [
    {
      "index": 0, "start": 0.0, "end": 25.0,
      "text": "本日はお忙しい中...",
      "cluster": "0:A",        // チャンク0の「声A」
      "chunk": 0,
      "speaker_id": "sp01",    // null なら未確定
      "reviewed": true,        // false = まとめて適用しただけ(未確認)
      "text_edited": false,    // true なら再実行時も本文を上書きしない
      "time_edited": false,    // true なら以後この区間にずれ補正を足さない
      "time_reviewed": false,  // true = 時刻を自分の耳で確かめた
      "orig_start": 0.0,       // AI が出した元の時刻。人が直しても動かさない
      "orig_end": 25.0
    }
  ]
}
```

`speaker_id` は名前ではなく ID を指しています。出席者の並び順を変えたり名簿を編集しても
ID は保たれるので、確定済みの区間が別人に化けることはありません。

---

## ビルド手順 (Windows・ローカル)

### 必要なもの

| ツール | 用途 | 入手先 |
| --- | --- | --- |
| Python 3.11 以上 | アプリ本体 | https://www.python.org/downloads/ (PATH 追加にチェック) |
| ffmpeg + ffplay(static build) | 音声分割・区間再生 | https://www.gyan.dev/ffmpeg/builds/ の **release-essentials** |
| 話者分離のモデル(44MB) | 声のまとまりを作る | `python tools\fetch_diarize_models.py`(手順 3) |
| Inno Setup 6 | インストーラ作成 | https://jrsoftware.org/isdl.php |
| Gemini API キー | 文字起こし | https://aistudio.google.com/apikey |

### 手順

1. **このフォルダを任意の場所に展開**します。

2. **ffmpeg.exe と ffplay.exe を配置**します。
   `ffmpeg-release-essentials.zip` を解凍 → `bin\` の 2 つをプロジェクトの `ffmpeg\` にコピー。
   ```
   whosaid-editor\
     ├─ ffmpeg\
     │   ├─ ffmpeg.exe   ← 必須
     │   └─ ffplay.exe   ← 推奨(無いと区間再生が簡易モードになります)
     ├─ src\
     ├─ build.bat
     └─ ...
   ```

3. **話者分離のモデルを取得**します。

   ```
   python tools\fetch_diarize_models.py
   ```

   `models\diarize\` に 2 つ（合計 44MB）置かれます。取得元と SHA-256 は
   スクリプトの中にあり、照合してから配置します。

   ```
   models\diarize\
     ├─ sherpa-onnx-pyannote-segmentation-3-0\model.onnx  ← 5.7MB・MIT
     └─ nemo_en_titanet_small.onnx                        ← 38.4MB・CC-BY-4.0
   ```

   リポジトリには含めていません（大きさとライセンスが別々のため）。
   **無いと `build.spec` がビルドを止めます。**入れ忘れたまま配ると、
   利用者の端末で初めて「話者分離のモデルが見つかりません」と出て、
   ローカル経路の区間がすべて `?` になります。

   > **TitaNet は CC-BY-4.0 で、配布物への表示が条件です。**
   > `resources\CREDITS.txt` がその表示にあたります。同梱は `build.spec` が
   > 行い、アプリ画面の［使っている部品と表示］から読めます。
   > このファイルが無い場合もビルドは止まります。

4. **(任意) アイコンを置く**: `resources\icon.ico`。

5. **`build.bat` をダブルクリック** → `Output\GeminiTranscriberSetup-2.1.0.exe`。

> Inno Setup を入れずにビルドした場合は、`dist\GeminiTranscriber\` をフォルダごと
> コピーすれば動きます(ポータブル運用)。

### GitHub Actions による自動ビルド

`main` への push、または `v*` タグの push で Windows ランナー上で自動ビルドが走ります。
テスト(`tests/`)も同じジョブで実行されます。

- **PR / push**: `Actions` タブから `GeminiTranscriber-Installer` artifact をダウンロード可能
- **タグ push** (`git tag v2.1.0 && git push --tags`): 自動で Release を作成し `.exe` を添付

---

## 開発時に直接動かす

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
# ffmpeg / ffplay を PATH に通すか、ffmpeg\ に配置
python -m src.main
```

### テスト

```powershell
python tests\test_core.py                  # データモデル・パーサ・候補学習
python tests\test_pipeline_integration.py  # 分割〜セグメント化(ffmpeg 必要 / API は呼ばない)
python tests\test_gui_smoke.py             # 割当画面の動作(画面が一瞬開きます)
```

---

## トラブルシューティング

| 症状 | 対処 |
| --- | --- |
| 起動時に「ffmpeg が見つかりません」 | `ffmpeg\ffmpeg.exe` を配置して再ビルド |
| 割当画面で音が出ない | `ffmpeg\ffplay.exe` を同梱して再ビルド。画面下部に現在の再生方式が出ます |
| 「音声ファイルが見つかりません」 | 音声を移動した場合。再生ボタンから場所を指定し直せます |
| 同じ人が別のまとまりに分かれる | 声のまとまりはチャンクごとに独立です。チャンク長を延ばすと減ります |
| 再生した音声とテキストがずれる | Gemini の時刻推定のずれです。「◀ 5秒前から」「この先30秒▶」で前後を確認してください |
| 一括適用を間違えた | `Ctrl+Z`、または「このまとまりを未確定に戻す」 |
| API キーエラー | https://aistudio.google.com/apikey で発行し、画面で「保存」 |
| 大きなファイルでアップロード失敗 | 詳細設定の「チャンク長」を短く(例: 5 分) |
| インストーラを Defender がブロック | コード署名を行うか、自己責任で除外。配布前に署名を推奨 |

---

## 注意

- Gemini の利用料金は本アプリでは制御していません。長尺ファイルのコストに留意してください。
  (v2.0 では名簿をプロンプトに含めない分、v1.3 よりわずかに安くなります)
- 音声ファイルは Gemini にアップロードされます。機密情報を含む場合は社内ポリシーを確認してください。
- ffmpeg / ffplay は LGPL/GPL ライセンスです。再配布する場合はライセンス表記を同梱してください。
